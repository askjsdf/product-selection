# Step 2 品类洞察阶段 - 前端实现计划

> **文档版本**: v1.0
> **创建日期**: 2025-11-19
> **适用范围**: Step 2 (品类洞察) 七子模块前端开发
> **技术栈**: HTML5 + Bootstrap 5 + ECharts 5 + Vanilla JavaScript

---

## 📋 目录

1. [实施策略](#实施策略)
2. [技术架构](#技术架构)
3. [Phase 分解](#phase-分解)
4. [数据Mock策略](#数据mock策略)
5. [开发规范](#开发规范)
6. [测试计划](#测试计划)
7. [风险控制](#风险控制)

---

## 🎯 实施策略

### 核心原则

```
✅ 递进式开发: 每个Phase完成后立即测试验证
✅ 数据驱动: 使用设计文档中的真实示例数据
✅ 组件复用: ECharts配置、卡片样式等抽象为可复用函数
✅ 模块解耦: 7个子模块可独立运行，通过数据传递联动
✅ 前端Demo: 不做后端接口，所有数据Mock在前端
```

### 实施路径

```
Phase 1: 五看洞察仪表板 (2.1)
   ↓ 数据基础 ↓
Phase 2: 市场地图 & STP聚焦 (2.2 + 2.3)
   ↓ 战略定位 ↓
Phase 3: SPAN矩阵 & $APPEALS分析 (2.4 + 2.5)
   ↓ 竞争评估 ↓
Phase 4: KANO需求分级引擎 (2.6)
   ↓ MVP优化 ↓
Phase 5: 数字化Charter生成器 (2.7)
   ↓ 最终交付 ↓
集成测试 & 优化
```

---

## 🏗️ 技术架构

### 文件结构

```
product-selection/
├── modules/
│   └── step2.html                    # Step 2 主容器 (重构)
├── assets/
│   ├── js/
│   │   ├── step2.js                  # Step 2 主逻辑 (重构)
│   │   ├── step2-data.js             # 数据Mock (新建)
│   │   ├── step2-charts.js           # ECharts配置 (新建)
│   │   └── step2-utils.js            # 工具函数 (新建)
│   └── css/
│       └── step2.css                 # Step 2 样式 (重构)
└── docs/design/从品类到产品的战略决策/
    ├── 2.1-五看洞察仪表板设计.md      # 设计参考
    ├── 2.2-数字化市场地图设计.md
    ├── 2.3-STP战略聚焦设计.md
    ├── 2.4-SPAN矩阵智能评估设计.md
    ├── 2.5-APPEALS差异化分析设计.md
    ├── 2.6-KANO需求分级引擎设计.md
    ├── 2.7-数字化Charter生成器设计.md
    └── 00-前端实现计划.md            # 本文档
```

### 模块架构

```javascript
// step2.js 模块结构
window.Step2 = {
  // 全局状态
  state: {
    currentCategory: null,        // 当前选中品类
    currentPlatform: null,        // 当前平台
    currentCountry: null,         // 当前国家
    currentSubModule: 'wukan',    // 当前子模块
    analysisData: {}              // 分析数据缓存
  },

  // 初始化函数
  init() { ... },

  // 子模块渲染函数
  modules: {
    wukan: { ... },               // Phase 1
    marketMap: { ... },           // Phase 2
    stp: { ... },                 // Phase 2
    span: { ... },                // Phase 3
    appeals: { ... },             // Phase 3
    kano: { ... },                // Phase 4
    charter: { ... }              // Phase 5
  },

  // 工具函数
  utils: {
    switchSubModule() { ... },
    saveAnalysisData() { ... },
    exportPDF() { ... }
  }
};
```

### 数据流设计

```
Step 1 候选池
    ↓
[品类选择器] → 选择"宠物用品 × 美国 × Amazon"
    ↓
Step2Data.getMockData('宠物用品') → 返回完整分析数据
    ↓
分发到各子模块:
  ├─ wukan: 五看数据
  ├─ marketMap: 市场地图数据
  ├─ stp: STP细分市场数据
  ├─ span: SPAN评分数据
  ├─ appeals: $APPEALS对比数据
  ├─ kano: KANO特征数据
  └─ charter: Charter整合数据
    ↓
渲染对应UI
```

---

## 📦 Phase 分解

### **Phase 1: 五看洞察仪表板** (优先级⭐⭐⭐⭐⭐)

#### 实现目标
实现完整的五看分析仪表板，作为整个 Step 2 的数据基础。

#### 界面结构

```html
<!-- modules/step2.html (Phase 1 版本) -->
<div class="step2-container">

  <!-- 1. 品类选择器 -->
  <div class="step2-header">
    <div class="category-selector">
      <label>选择品类组合:</label>
      <select id="category-combo-select">
        <!-- 从候选池读取 -->
        <option value="宠物用品|美国|Amazon">宠物用品 × 美国 × Amazon</option>
        <option value="家居装饰|英国|eBay">家居装饰 × 英国 × eBay</option>
      </select>
    </div>
    <div class="analysis-actions">
      <button class="btn btn-outline btn-sm" id="refresh-data-btn">
        🔄 刷新数据
      </button>
      <button class="btn btn-primary btn-sm" id="export-wukan-btn">
        📥 导出五看分析
      </button>
    </div>
  </div>

  <!-- 2. 子模块Tab导航 -->
  <ul class="sub-module-tabs" role="tablist">
    <li class="active" data-module="wukan">
      <span class="module-icon">👀</span>
      <span class="module-name">五看洞察</span>
    </li>
    <li data-module="market-stp" disabled>
      <span class="module-icon">🗺️</span>
      <span class="module-name">市场地图 & STP</span>
      <span class="badge badge-sm">Phase 2</span>
    </li>
    <li data-module="span-appeals" disabled>
      <span class="module-icon">📊</span>
      <span class="module-name">SPAN & $APPEALS</span>
      <span class="badge badge-sm">Phase 3</span>
    </li>
    <li data-module="kano" disabled>
      <span class="module-icon">🎯</span>
      <span class="module-name">KANO分级</span>
      <span class="badge badge-sm">Phase 4</span>
    </li>
    <li data-module="charter" disabled>
      <span class="module-icon">📋</span>
      <span class="module-name">Charter生成</span>
      <span class="badge badge-sm">Phase 5</span>
    </li>
  </ul>

  <!-- 3. 五看洞察内容区 -->
  <div id="wukan-module" class="sub-module-content">

    <!-- 五看Tab导航 -->
    <ul class="wukan-tabs" role="tablist">
      <li class="active" data-wukan-tab="industry">
        <span class="tab-number">1</span>
        <span class="tab-name">看行业</span>
      </li>
      <li data-wukan-tab="market">
        <span class="tab-number">2</span>
        <span class="tab-name">看市场</span>
      </li>
      <li data-wukan-tab="customer">
        <span class="tab-number">3</span>
        <span class="tab-name">看客户</span>
      </li>
      <li data-wukan-tab="competition">
        <span class="tab-number">4</span>
        <span class="tab-name">看竞争</span>
      </li>
      <li data-wukan-tab="resource">
        <span class="tab-number">5</span>
        <span class="tab-name">看自己</span>
      </li>
    </ul>

    <!-- 五看内容面板 -->
    <div class="wukan-panels">

      <!-- Panel 1: 看行业 -->
      <div id="industry-panel" class="wukan-panel active">
        <div class="panel-grid">
          <div class="metric-card">
            <div class="metric-label">市场规模 (GMV)</div>
            <div class="metric-value" id="industry-gmv">$18.7B</div>
            <div class="metric-trend positive">+28.4% YoY</div>
          </div>
          <div class="metric-card">
            <div class="metric-label">年增长率 (CAGR)</div>
            <div class="metric-value" id="industry-cagr">28.4%</div>
            <div class="metric-trend">2023-2028</div>
          </div>
          <div class="metric-card">
            <div class="metric-label">成熟度</div>
            <div class="metric-value" id="industry-maturity">成长期</div>
            <div class="metric-trend">快速扩张中</div>
          </div>
        </div>

        <div class="chart-row">
          <div class="chart-container">
            <h4>品类结构分布</h4>
            <div id="industry-structure-chart" style="height: 300px;"></div>
          </div>
          <div class="chart-container">
            <h4>增长趋势 (2020-2028)</h4>
            <div id="industry-growth-chart" style="height: 300px;"></div>
          </div>
        </div>
      </div>

      <!-- Panel 2: 看市场 -->
      <div id="market-panel" class="wukan-panel">
        <div class="subcategory-grid">
          <!-- 5个子品类卡片 -->
          <div class="subcategory-card" data-subcategory="玩具娱乐">
            <div class="subcategory-header">
              <h4>玩具娱乐</h4>
              <span class="badge badge-high-growth">高增长</span>
            </div>
            <div class="subcategory-metrics">
              <div class="metric">
                <span class="metric-label">GMV</span>
                <span class="metric-value">$2.81B</span>
              </div>
              <div class="metric">
                <span class="metric-label">增长率</span>
                <span class="metric-value positive">67%</span>
              </div>
              <div class="metric">
                <span class="metric-label">市场份额</span>
                <span class="metric-value">15%</span>
              </div>
            </div>
          </div>
          <!-- 其他4个子品类... -->
        </div>

        <div class="chart-container">
          <h4>子品类五维雷达对比</h4>
          <div id="market-radar-chart" style="height: 400px;"></div>
        </div>
      </div>

      <!-- Panel 3: 看客户 -->
      <div id="customer-panel" class="wukan-panel">
        <h3>客户画像 (Top 3 Segments)</h3>
        <div class="persona-grid">
          <div class="persona-card">
            <div class="persona-avatar">👩‍💼</div>
            <h4>品质妈妈</h4>
            <div class="persona-tags">
              <span class="tag">30-45岁</span>
              <span class="tag">家庭年收入$80K+</span>
              <span class="tag">一线城市</span>
            </div>
            <div class="persona-metrics">
              <div>占比: <strong>42%</strong></div>
              <div>ARPU: <strong>$186/年</strong></div>
              <div>复购率: <strong>73%</strong></div>
            </div>
          </div>
          <!-- 其他2个画像... -->
        </div>

        <div class="pain-points-section">
          <h4>Top 5 客户痛点 (从50万条评论挖掘)</h4>
          <div class="pain-point-list">
            <div class="pain-point-item">
              <div class="rank">1</div>
              <div class="content">
                <div class="title">玩具耐用性差</div>
                <div class="stats">
                  <span>提及率: 34.2%</span>
                  <span>平均评分: 2.8⭐</span>
                </div>
                <div class="quote">"我家狗一天就咬坏了..."</div>
              </div>
            </div>
            <!-- 其他4个痛点... -->
          </div>
        </div>
      </div>

      <!-- Panel 4: 看竞争 -->
      <div id="competition-panel" class="wukan-panel">
        <div class="competition-metrics">
          <div class="metric-card">
            <div class="metric-label">CR5集中度</div>
            <div class="metric-value">35%</div>
            <div class="metric-trend">竞争分散</div>
          </div>
          <div class="metric-card">
            <div class="metric-label">Top品牌</div>
            <div class="metric-value">KONG</div>
            <div class="metric-trend">12%市场份额</div>
          </div>
          <div class="metric-card">
            <div class="metric-label">新品牌生存率</div>
            <div class="metric-value">67%</div>
            <div class="metric-trend">12个月存活</div>
          </div>
        </div>

        <div class="chart-row">
          <div class="chart-container">
            <h4>竞争格局雷达图</h4>
            <div id="competition-radar-chart" style="height: 350px;"></div>
          </div>
          <div class="competitor-table-container">
            <h4>Top 5 竞争对手</h4>
            <table class="competitor-table">
              <thead>
                <tr>
                  <th>品牌</th>
                  <th>市场份额</th>
                  <th>均价</th>
                  <th>评分</th>
                  <th>SKU数</th>
                </tr>
              </thead>
              <tbody id="competitor-table-body">
                <!-- 动态填充 -->
              </tbody>
            </table>
          </div>
        </div>
      </div>

      <!-- Panel 5: 看自己 -->
      <div id="resource-panel" class="wukan-panel">
        <h3>资源匹配度评估</h3>
        <div class="resource-score-card">
          <div class="score-circle">
            <div class="score-value">78</div>
            <div class="score-label">综合匹配度</div>
          </div>
          <div class="score-verdict">
            <span class="verdict-icon">✅</span>
            <span class="verdict-text">适合进入</span>
            <p class="verdict-desc">资源匹配度良好，建议从智力训练细分切入</p>
          </div>
        </div>

        <div class="resource-dimensions">
          <div class="dimension-item">
            <div class="dimension-header">
              <span class="dimension-name">资金能力</span>
              <span class="dimension-score">85/100</span>
            </div>
            <div class="progress-bar">
              <div class="progress-fill" style="width: 85%;"></div>
            </div>
            <div class="dimension-detail">
              启动资金需求: $25K | 你的预算: $35K ✅
            </div>
          </div>

          <div class="dimension-item">
            <div class="dimension-header">
              <span class="dimension-name">供应链能力</span>
              <span class="dimension-score">72/100</span>
            </div>
            <div class="progress-bar">
              <div class="progress-fill" style="width: 72%;"></div>
            </div>
            <div class="dimension-detail">
              MOQ: 500件 | 交期: 30天 | 质量认证: FDA食品级 ⚠️
            </div>
          </div>

          <!-- 其他维度... -->
        </div>
      </div>

    </div>
  </div>

</div>
```

#### JavaScript实现

```javascript
// assets/js/step2.js (Phase 1)

/**
 * Step 2 品类洞察 - 五看洞察模块
 */
window.Step2 = {

  // ========== 状态管理 ==========
  state: {
    currentCategory: null,
    currentPlatform: null,
    currentCountry: null,
    currentWukanTab: 'industry', // 当前五看Tab
    wukanData: null
  },

  // ========== 初始化 ==========
  init() {
    console.log('Step 2 - 五看洞察模块初始化');

    // 1. 从候选池加载品类选择器
    this.loadCategorySelector();

    // 2. 加载默认品类数据
    const defaultCategory = this.getDefaultCategory();
    if (defaultCategory) {
      this.loadCategoryData(defaultCategory);
    }

    // 3. 绑定事件
    this.bindEvents();
  },

  // ========== 品类选择器 ==========
  loadCategorySelector() {
    const pool = AppStorage.getOpportunityPool();
    const select = document.getElementById('category-combo-select');

    if (!select || pool.length === 0) {
      Utils.showWarningMessage('候选池为空，请先在 Step 1 添加组合');
      return;
    }

    select.innerHTML = pool.map(item => {
      const value = `${item.category}|${item.country}|${item.platform}`;
      const label = `${item.category} × ${item.country} × ${item.platform}`;
      return `<option value="${value}">${label}</option>`;
    }).join('');

    // 监听变化
    select.addEventListener('change', (e) => {
      const [category, country, platform] = e.target.value.split('|');
      this.loadCategoryData({ category, country, platform });
    });
  },

  getDefaultCategory() {
    const pool = AppStorage.getOpportunityPool();
    if (pool.length === 0) return null;

    return {
      category: pool[0].category,
      country: pool[0].country,
      platform: pool[0].platform
    };
  },

  // ========== 加载品类数据 ==========
  loadCategoryData({ category, country, platform }) {
    console.log(`加载品类数据: ${category} × ${country} × ${platform}`);

    // 更新状态
    this.state.currentCategory = category;
    this.state.currentCountry = country;
    this.state.currentPlatform = platform;

    // 从Mock数据获取
    this.state.wukanData = Step2Data.getWuKanData(category);

    // 渲染当前Tab
    this.renderCurrentWukanTab();
  },

  // ========== 渲染五看Tab ==========
  renderCurrentWukanTab() {
    const tab = this.state.currentWukanTab;

    // 隐藏所有面板
    document.querySelectorAll('.wukan-panel').forEach(panel => {
      panel.classList.remove('active');
    });

    // 显示当前面板
    const currentPanel = document.getElementById(`${tab}-panel`);
    if (currentPanel) {
      currentPanel.classList.add('active');
    }

    // 渲染对应内容
    switch(tab) {
      case 'industry':
        this.renderIndustryView();
        break;
      case 'market':
        this.renderMarketView();
        break;
      case 'customer':
        this.renderCustomerView();
        break;
      case 'competition':
        this.renderCompetitionView();
        break;
      case 'resource':
        this.renderResourceView();
        break;
    }
  },

  // ========== 1. 看行业 ==========
  renderIndustryView() {
    const data = this.state.wukanData.industry;

    // 更新指标
    document.getElementById('industry-gmv').textContent = data.gmv;
    document.getElementById('industry-cagr').textContent = data.cagr;
    document.getElementById('industry-maturity').textContent = data.maturity;

    // 饼图: 品类结构
    const structureChart = echarts.init(document.getElementById('industry-structure-chart'));
    structureChart.setOption(Step2Charts.getPieChartOption({
      title: '',
      data: data.subcategories.map(sub => ({
        name: sub.name,
        value: parseFloat(sub.share)
      }))
    }));

    // 折线图: 增长趋势
    const growthChart = echarts.init(document.getElementById('industry-growth-chart'));
    growthChart.setOption(Step2Charts.getLineChartOption({
      xData: data.growthTrend.years,
      yData: data.growthTrend.gmv,
      title: ''
    }));
  },

  // ========== 2. 看市场 ==========
  renderMarketView() {
    const data = this.state.wukanData.market;

    // 渲染子品类卡片
    const grid = document.querySelector('.subcategory-grid');
    grid.innerHTML = data.subcategories.map(sub => `
      <div class="subcategory-card ${sub.isHighGrowth ? 'highlight' : ''}">
        <div class="subcategory-header">
          <h4>${sub.name}</h4>
          ${sub.isHighGrowth ? '<span class="badge badge-high-growth">高增长</span>' : ''}
        </div>
        <div class="subcategory-metrics">
          <div class="metric">
            <span class="metric-label">GMV</span>
            <span class="metric-value">${sub.gmv}</span>
          </div>
          <div class="metric">
            <span class="metric-label">增长率</span>
            <span class="metric-value ${sub.growth > 30 ? 'positive' : ''}">${sub.growth}%</span>
          </div>
          <div class="metric">
            <span class="metric-label">市场份额</span>
            <span class="metric-value">${sub.share}</span>
          </div>
        </div>
      </div>
    `).join('');

    // 雷达图: 子品类对比
    const radarChart = echarts.init(document.getElementById('market-radar-chart'));
    radarChart.setOption(Step2Charts.getRadarChartOption({
      indicator: [
        { name: '市场规模', max: 100 },
        { name: '增长潜力', max: 100 },
        { name: '利润空间', max: 100 },
        { name: '竞争强度', max: 100 },
        { name: '进入难度', max: 100 }
      ],
      series: data.subcategories.map(sub => ({
        name: sub.name,
        value: sub.radarScores
      }))
    }));
  },

  // ========== 3. 看客户 ==========
  renderCustomerView() {
    const data = this.state.wukanData.customer;

    // 渲染客户画像
    const personaGrid = document.querySelector('.persona-grid');
    personaGrid.innerHTML = data.personas.map(persona => `
      <div class="persona-card">
        <div class="persona-avatar">${persona.avatar}</div>
        <h4>${persona.name}</h4>
        <div class="persona-tags">
          ${persona.tags.map(tag => `<span class="tag">${tag}</span>`).join('')}
        </div>
        <div class="persona-metrics">
          <div>占比: <strong>${persona.share}</strong></div>
          <div>ARPU: <strong>${persona.arpu}</strong></div>
          <div>复购率: <strong>${persona.retention}</strong></div>
        </div>
        <div class="persona-desc">${persona.description}</div>
      </div>
    `).join('');

    // 渲染痛点列表
    const painPointList = document.querySelector('.pain-point-list');
    painPointList.innerHTML = data.painPoints.map((point, index) => `
      <div class="pain-point-item">
        <div class="rank">${index + 1}</div>
        <div class="content">
          <div class="title">${point.title}</div>
          <div class="stats">
            <span>提及率: ${point.mentionRate}</span>
            <span>平均评分: ${point.avgRating}⭐</span>
          </div>
          <div class="quote">"${point.quote}"</div>
        </div>
      </div>
    `).join('');
  },

  // ========== 4. 看竞争 ==========
  renderCompetitionView() {
    const data = this.state.wukanData.competition;

    // 竞争指标
    // (从DOM获取对应元素更新值)

    // 竞争雷达图
    const radarChart = echarts.init(document.getElementById('competition-radar-chart'));
    radarChart.setOption(Step2Charts.getRadarChartOption({
      indicator: [
        { name: '品牌知名度', max: 100 },
        { name: '产品质量', max: 100 },
        { name: '价格竞争力', max: 100 },
        { name: '客户满意度', max: 100 },
        { name: 'SKU丰富度', max: 100 }
      ],
      series: data.topCompetitors.map(comp => ({
        name: comp.brand,
        value: comp.radarScores
      }))
    }));

    // 竞争对手表格
    const tbody = document.getElementById('competitor-table-body');
    tbody.innerHTML = data.topCompetitors.map(comp => `
      <tr>
        <td><strong>${comp.brand}</strong></td>
        <td>${comp.marketShare}</td>
        <td>${comp.avgPrice}</td>
        <td>${comp.rating}⭐</td>
        <td>${comp.skuCount}</td>
      </tr>
    `).join('');
  },

  // ========== 5. 看自己 ==========
  renderResourceView() {
    const data = this.state.wukanData.resource;

    // 综合得分
    document.querySelector('.score-value').textContent = data.overallScore;
    document.querySelector('.verdict-text').textContent = data.verdict;
    document.querySelector('.verdict-desc').textContent = data.recommendation;

    // 资源维度
    const dimensionsContainer = document.querySelector('.resource-dimensions');
    dimensionsContainer.innerHTML = data.dimensions.map(dim => `
      <div class="dimension-item">
        <div class="dimension-header">
          <span class="dimension-name">${dim.name}</span>
          <span class="dimension-score">${dim.score}/100</span>
        </div>
        <div class="progress-bar">
          <div class="progress-fill" style="width: ${dim.score}%;"></div>
        </div>
        <div class="dimension-detail">${dim.detail}</div>
      </div>
    `).join('');
  },

  // ========== 事件绑定 ==========
  bindEvents() {
    // 五看Tab切换
    document.querySelectorAll('.wukan-tabs li').forEach(tab => {
      tab.addEventListener('click', (e) => {
        const targetTab = e.currentTarget.dataset.wukanTab;

        // 更新Tab状态
        document.querySelectorAll('.wukan-tabs li').forEach(t => t.classList.remove('active'));
        e.currentTarget.classList.add('active');

        // 更新状态并渲染
        this.state.currentWukanTab = targetTab;
        this.renderCurrentWukanTab();
      });
    });

    // 刷新数据按钮
    document.getElementById('refresh-data-btn')?.addEventListener('click', () => {
      this.loadCategoryData({
        category: this.state.currentCategory,
        country: this.state.currentCountry,
        platform: this.state.currentPlatform
      });
      Utils.showSuccessMessage('数据已刷新');
    });

    // 导出按钮
    document.getElementById('export-wukan-btn')?.addEventListener('click', () => {
      this.exportWuKanAnalysis();
    });
  },

  // ========== 导出功能 ==========
  exportWuKanAnalysis() {
    const data = this.state.wukanData;
    const category = this.state.currentCategory;

    // 构建导出数据
    const exportData = {
      category: category,
      timestamp: new Date().toISOString(),
      analysis: data
    };

    // 下载JSON
    const blob = new Blob([JSON.stringify(exportData, null, 2)], { type: 'application/json' });
    const url = URL.createObjectURL(blob);
    const a = document.createElement('a');
    a.href = url;
    a.download = `五看分析_${category}_${Date.now()}.json`;
    a.click();
    URL.revokeObjectURL(url);

    Utils.showSuccessMessage('五看分析已导出');
  }
};

// 页面加载完成后初始化
if (document.readyState === 'loading') {
  document.addEventListener('DOMContentLoaded', () => Step2.init());
} else {
  Step2.init();
}
```

#### 数据Mock文件

```javascript
// assets/js/step2-data.js

/**
 * Step 2 Mock数据
 * 数据来源: docs/design/从品类到产品的战略决策/2.1-五看洞察仪表板设计.md
 */
window.Step2Data = {

  /**
   * 获取五看数据
   */
  getWuKanData(category) {
    const dataMap = {
      "宠物用品": {
        industry: {
          gmv: "$18.7B",
          cagr: "28.4%",
          maturity: "成长期",
          subcategories: [
            { name: "宠物食品", share: "42%", gmv: "$7.85B" },
            { name: "健康护理", share: "23%", gmv: "$4.30B" },
            { name: "玩具娱乐", share: "15%", gmv: "$2.81B" },
            { name: "家居用品", share: "12%", gmv: "$2.24B" },
            { name: "清洁用品", share: "8%", gmv: "$1.50B" }
          ],
          growthTrend: {
            years: ['2020', '2021', '2022', '2023', '2024E', '2025E', '2026E', '2027E', '2028E'],
            gmv: [8.2, 10.5, 12.8, 14.6, 18.7, 24.0, 30.8, 39.5, 50.7]
          }
        },
        market: {
          subcategories: [
            {
              name: "玩具娱乐",
              gmv: "$2.81B",
              share: "15%",
              growth: 67,
              isHighGrowth: true,
              radarScores: [60, 95, 72, 65, 70]
            },
            {
              name: "宠物食品",
              gmv: "$7.85B",
              share: "42%",
              growth: 18,
              isHighGrowth: false,
              radarScores: [95, 45, 55, 85, 40]
            },
            {
              name: "健康护理",
              gmv: "$4.30B",
              share: "23%",
              growth: 35,
              isHighGrowth: true,
              radarScores: [75, 70, 80, 70, 55]
            },
            {
              name: "家居用品",
              gmv: "$2.24B",
              share: "12%",
              growth: 22,
              isHighGrowth: false,
              radarScores: [50, 55, 60, 60, 65]
            },
            {
              name: "清洁用品",
              gmv: "$1.50B",
              share: "8%",
              growth: 15,
              isHighGrowth: false,
              radarScores: [40, 40, 50, 55, 70]
            }
          ]
        },
        customer: {
          personas: [
            {
              avatar: "👩‍💼",
              name: "品质妈妈",
              tags: ["30-45岁", "家庭年收入$80K+", "一线城市"],
              share: "42%",
              arpu: "$186/年",
              retention: "73%",
              description: "追求高品质宠物产品,愿意为科学喂养支付溢价"
            },
            {
              avatar: "👨‍💻",
              name: "年轻专业人士",
              tags: ["25-35岁", "单身/新婚", "互联网从业"],
              share: "35%",
              arpu: "$142/年",
              retention: "68%",
              description: "重视便捷性和设计感,活跃在社交媒体分享宠物日常"
            },
            {
              avatar: "👵",
              name: "银发宠物家长",
              tags: ["55-70岁", "退休/半退休", "子女独立"],
              share: "23%",
              arpu: "$98/年",
              retention: "82%",
              description: "宠物是情感寄托,注重安全性和实用性,价格敏感度低"
            }
          ],
          painPoints: [
            {
              title: "玩具耐用性差",
              mentionRate: "34.2%",
              avgRating: "2.8",
              quote: "我家狗一天就咬坏了,太不耐用"
            },
            {
              title: "缺乏智力刺激",
              mentionRate: "28.7%",
              avgRating: "3.1",
              quote: "狗狗玩5分钟就没兴趣了,需要更有挑战性的玩具"
            },
            {
              title: "清洁困难",
              mentionRate: "23.5%",
              avgRating: "3.3",
              quote: "KONG的缝隙太难洗,发霉了很恶心"
            },
            {
              title: "尺寸不合适",
              mentionRate: "19.8%",
              avgRating: "3.5",
              quote: "买大了狗狗咬不动,买小了吞下去危险"
            },
            {
              title: "价格偏高",
              mentionRate: "15.3%",
              avgRating: "3.8",
              quote: "一个玩具$25太贵了,性价比不高"
            }
          ]
        },
        competition: {
          cr5: "35%",
          topBrand: "KONG",
          topBrandShare: "12%",
          newBrandSurvivalRate: "67%",
          topCompetitors: [
            {
              brand: "KONG",
              marketShare: "12%",
              avgPrice: "$18.99",
              rating: "4.7",
              skuCount: "230+",
              radarScores: [95, 92, 70, 88, 95]
            },
            {
              brand: "Outward Hound",
              marketShare: "8%",
              avgPrice: "$15.49",
              rating: "4.5",
              skuCount: "180+",
              radarScores: [75, 78, 85, 82, 80]
            },
            {
              brand: "Chuckit!",
              marketShare: "7%",
              avgPrice: "$12.99",
              rating: "4.6",
              skuCount: "95+",
              radarScores: [70, 85, 90, 80, 60]
            },
            {
              brand: "ZippyPaws",
              marketShare: "5%",
              avgPrice: "$9.99",
              rating: "4.4",
              skuCount: "150+",
              radarScores: [50, 65, 95, 75, 70]
            },
            {
              brand: "Benebone",
              marketShare: "3%",
              avgPrice: "$14.99",
              rating: "4.5",
              skuCount: "45+",
              radarScores: [55, 88, 75, 85, 50]
            }
          ]
        },
        resource: {
          overallScore: 78,
          verdict: "✅ 适合进入",
          recommendation: "资源匹配度良好,建议从智力训练细分切入",
          dimensions: [
            {
              name: "资金能力",
              score: 85,
              detail: "启动资金需求: $25K | 你的预算: $35K ✅"
            },
            {
              name: "供应链能力",
              score: 72,
              detail: "MOQ: 500件 | 交期: 30天 | 质量认证: FDA食品级 ⚠️"
            },
            {
              name: "运营能力",
              score: 80,
              detail: "需要: Amazon PPC + 社交媒体营销 | 你的经验: 2年电商运营 ✅"
            },
            {
              name: "产品开发能力",
              score: 68,
              detail: "需要: 产品设计 + 模具开发 | 建议: 寻找ODM合作伙伴 ⚠️"
            },
            {
              name: "时间投入",
              score: 75,
              detail: "启动期需要: 20小时/周 | 你的可用时间: 25小时/周 ✅"
            }
          ]
        }
      },

      // 其他品类数据可以后续补充
      "家居装饰": {
        // TODO: 补充家居装饰品类数据
      }
    };

    return dataMap[category] || dataMap["宠物用品"]; // 默认返回宠物用品
  }
};
```

#### ECharts配置文件

```javascript
// assets/js/step2-charts.js

/**
 * Step 2 图表配置
 */
window.Step2Charts = {

  // 深色主题配置
  darkTheme: {
    backgroundColor: 'transparent',
    textStyle: {
      color: '#e4e4e7'
    },
    tooltip: {
      backgroundColor: 'rgba(24, 24, 27, 0.95)',
      borderColor: '#3f3f46',
      textStyle: { color: '#e4e4e7' }
    }
  },

  /**
   * 饼图配置
   */
  getPieChartOption({ title, data }) {
    return {
      ...this.darkTheme,
      title: {
        text: title,
        left: 'center',
        textStyle: { color: '#e4e4e7' }
      },
      tooltip: {
        trigger: 'item',
        formatter: '{b}: {c} ({d}%)'
      },
      legend: {
        orient: 'vertical',
        right: 10,
        top: 'center',
        textStyle: { color: '#e4e4e7' }
      },
      series: [{
        type: 'pie',
        radius: '60%',
        data: data,
        emphasis: {
          itemStyle: {
            shadowBlur: 10,
            shadowOffsetX: 0,
            shadowColor: 'rgba(0, 0, 0, 0.5)'
          }
        }
      }]
    };
  },

  /**
   * 折线图配置
   */
  getLineChartOption({ title, xData, yData }) {
    return {
      ...this.darkTheme,
      title: {
        text: title,
        left: 'center',
        textStyle: { color: '#e4e4e7' }
      },
      tooltip: {
        trigger: 'axis'
      },
      xAxis: {
        type: 'category',
        data: xData,
        axisLine: { lineStyle: { color: '#3f3f46' } },
        axisLabel: { color: '#a1a1aa' }
      },
      yAxis: {
        type: 'value',
        axisLine: { lineStyle: { color: '#3f3f46' } },
        axisLabel: { color: '#a1a1aa' },
        splitLine: { lineStyle: { color: '#27272a' } }
      },
      series: [{
        data: yData,
        type: 'line',
        smooth: true,
        itemStyle: { color: '#3b82f6' },
        areaStyle: {
          color: {
            type: 'linear',
            x: 0, y: 0, x2: 0, y2: 1,
            colorStops: [
              { offset: 0, color: 'rgba(59, 130, 246, 0.3)' },
              { offset: 1, color: 'rgba(59, 130, 246, 0)' }
            ]
          }
        }
      }]
    };
  },

  /**
   * 雷达图配置
   */
  getRadarChartOption({ indicator, series }) {
    return {
      ...this.darkTheme,
      tooltip: {
        trigger: 'item'
      },
      legend: {
        bottom: 10,
        textStyle: { color: '#e4e4e7' }
      },
      radar: {
        indicator: indicator,
        splitArea: {
          areaStyle: {
            color: ['rgba(59, 130, 246, 0.05)', 'rgba(59, 130, 246, 0.1)']
          }
        },
        axisLine: { lineStyle: { color: '#3f3f46' } },
        splitLine: { lineStyle: { color: '#3f3f46' } }
      },
      series: [{
        type: 'radar',
        data: series.map(item => ({
          name: item.name,
          value: item.value
        }))
      }]
    };
  }
};
```

#### CSS样式

```css
/* assets/css/step2.css (Phase 1) */

/* ========== Step 2 容器 ========== */
.step2-container {
  display: flex;
  flex-direction: column;
  height: 100%;
  gap: var(--spacing-md);
}

/* ========== 品类选择器 ========== */
.step2-header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding: var(--spacing-md);
  background-color: var(--bg-secondary);
  border-radius: var(--border-radius-lg);
}

.category-selector {
  display: flex;
  align-items: center;
  gap: var(--spacing-sm);
}

.category-selector label {
  font-weight: 600;
  color: var(--text-secondary);
}

.category-selector select {
  min-width: 300px;
  padding: var(--spacing-sm) var(--spacing-md);
  background-color: var(--bg-primary);
  color: var(--text-primary);
  border: 1px solid var(--border-color);
  border-radius: var(--border-radius-md);
  font-size: var(--font-size-md);
}

.analysis-actions {
  display: flex;
  gap: var(--spacing-sm);
}

/* ========== 子模块Tab ========== */
.sub-module-tabs {
  display: flex;
  gap: var(--spacing-sm);
  padding: 0;
  margin: 0;
  list-style: none;
  background-color: var(--bg-secondary);
  border-radius: var(--border-radius-lg);
  overflow-x: auto;
}

.sub-module-tabs li {
  flex: 1;
  min-width: 160px;
  padding: var(--spacing-md);
  text-align: center;
  cursor: pointer;
  border-radius: var(--border-radius-lg);
  transition: all 0.2s;
}

.sub-module-tabs li:hover {
  background-color: rgba(59, 130, 246, 0.1);
}

.sub-module-tabs li.active {
  background-color: var(--color-primary);
  color: white;
}

.sub-module-tabs li[disabled] {
  opacity: 0.5;
  cursor: not-allowed;
}

.module-icon {
  display: block;
  font-size: 24px;
  margin-bottom: var(--spacing-xs);
}

.module-name {
  display: block;
  font-size: var(--font-size-sm);
  font-weight: 600;
}

/* ========== 五看Tab ========== */
.wukan-tabs {
  display: flex;
  gap: var(--spacing-xs);
  padding: 0;
  margin: 0 0 var(--spacing-lg) 0;
  list-style: none;
  background-color: var(--bg-secondary);
  border-radius: var(--border-radius-lg);
}

.wukan-tabs li {
  flex: 1;
  display: flex;
  align-items: center;
  justify-content: center;
  gap: var(--spacing-sm);
  padding: var(--spacing-md);
  cursor: pointer;
  border-radius: var(--border-radius-lg);
  transition: all 0.2s;
}

.wukan-tabs li:hover {
  background-color: rgba(59, 130, 246, 0.1);
}

.wukan-tabs li.active {
  background-color: var(--color-primary);
  color: white;
}

.tab-number {
  display: flex;
  align-items: center;
  justify-content: center;
  width: 24px;
  height: 24px;
  background-color: rgba(255, 255, 255, 0.2);
  border-radius: 50%;
  font-size: var(--font-size-sm);
  font-weight: 700;
}

.tab-name {
  font-size: var(--font-size-md);
  font-weight: 600;
}

/* ========== 五看面板 ========== */
.wukan-panels {
  flex: 1;
  overflow-y: auto;
}

.wukan-panel {
  display: none;
  animation: fadeIn 0.3s;
}

.wukan-panel.active {
  display: block;
}

@keyframes fadeIn {
  from { opacity: 0; transform: translateY(10px); }
  to { opacity: 1; transform: translateY(0); }
}

/* ========== 指标卡片 ========== */
.panel-grid {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
  gap: var(--spacing-md);
  margin-bottom: var(--spacing-lg);
}

.metric-card {
  padding: var(--spacing-lg);
  background-color: var(--bg-secondary);
  border-radius: var(--border-radius-lg);
  border: 1px solid var(--border-color);
}

.metric-label {
  font-size: var(--font-size-sm);
  color: var(--text-muted);
  margin-bottom: var(--spacing-xs);
}

.metric-value {
  font-size: var(--font-size-xxl);
  font-weight: 700;
  color: var(--text-primary);
  margin-bottom: var(--spacing-xs);
}

.metric-trend {
  font-size: var(--font-size-sm);
  color: var(--text-secondary);
}

.metric-trend.positive {
  color: var(--color-success);
}

.metric-trend.negative {
  color: var(--color-danger);
}

/* ========== 图表容器 ========== */
.chart-row {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(400px, 1fr));
  gap: var(--spacing-lg);
  margin-top: var(--spacing-lg);
}

.chart-container {
  padding: var(--spacing-lg);
  background-color: var(--bg-secondary);
  border-radius: var(--border-radius-lg);
  border: 1px solid var(--border-color);
}

.chart-container h4 {
  margin-bottom: var(--spacing-md);
  font-size: var(--font-size-lg);
  color: var(--text-primary);
}

/* ========== 子品类卡片 ========== */
.subcategory-grid {
  display: grid;
  grid-template-columns: repeat(auto-fill, minmax(280px, 1fr));
  gap: var(--spacing-md);
  margin-bottom: var(--spacing-lg);
}

.subcategory-card {
  padding: var(--spacing-lg);
  background-color: var(--bg-secondary);
  border-radius: var(--border-radius-lg);
  border: 1px solid var(--border-color);
  transition: all 0.2s;
}

.subcategory-card:hover {
  border-color: var(--color-primary);
  box-shadow: 0 4px 12px rgba(59, 130, 246, 0.2);
}

.subcategory-card.highlight {
  border-color: var(--color-success);
  background: linear-gradient(135deg, rgba(34, 197, 94, 0.1), transparent);
}

.subcategory-header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  margin-bottom: var(--spacing-md);
}

.subcategory-header h4 {
  font-size: var(--font-size-lg);
  color: var(--text-primary);
}

.badge-high-growth {
  background-color: var(--color-success);
  color: white;
  padding: 2px 8px;
  border-radius: var(--border-radius-sm);
  font-size: var(--font-size-xs);
}

.subcategory-metrics {
  display: flex;
  flex-direction: column;
  gap: var(--spacing-sm);
}

.subcategory-metrics .metric {
  display: flex;
  justify-content: space-between;
  align-items: center;
}

/* ========== 客户画像 ========== */
.persona-grid {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
  gap: var(--spacing-lg);
  margin-bottom: var(--spacing-xl);
}

.persona-card {
  padding: var(--spacing-lg);
  background-color: var(--bg-secondary);
  border-radius: var(--border-radius-lg);
  border: 1px solid var(--border-color);
  text-align: center;
}

.persona-avatar {
  font-size: 60px;
  margin-bottom: var(--spacing-md);
}

.persona-card h4 {
  font-size: var(--font-size-xl);
  color: var(--text-primary);
  margin-bottom: var(--spacing-sm);
}

.persona-tags {
  display: flex;
  flex-wrap: wrap;
  gap: var(--spacing-xs);
  justify-content: center;
  margin-bottom: var(--spacing-md);
}

.tag {
  padding: 4px 8px;
  background-color: var(--bg-primary);
  color: var(--text-secondary);
  border-radius: var(--border-radius-sm);
  font-size: var(--font-size-xs);
}

.persona-metrics {
  display: flex;
  flex-direction: column;
  gap: var(--spacing-xs);
  padding-top: var(--spacing-md);
  border-top: 1px solid var(--border-color);
  font-size: var(--font-size-sm);
  color: var(--text-secondary);
}

.persona-desc {
  margin-top: var(--spacing-md);
  font-size: var(--font-size-sm);
  color: var(--text-muted);
  font-style: italic;
}

/* ========== 痛点列表 ========== */
.pain-points-section {
  margin-top: var(--spacing-xl);
}

.pain-points-section h4 {
  font-size: var(--font-size-lg);
  color: var(--text-primary);
  margin-bottom: var(--spacing-md);
}

.pain-point-list {
  display: flex;
  flex-direction: column;
  gap: var(--spacing-md);
}

.pain-point-item {
  display: flex;
  gap: var(--spacing-md);
  padding: var(--spacing-lg);
  background-color: var(--bg-secondary);
  border-radius: var(--border-radius-lg);
  border: 1px solid var(--border-color);
}

.pain-point-item .rank {
  flex-shrink: 0;
  display: flex;
  align-items: center;
  justify-content: center;
  width: 40px;
  height: 40px;
  background-color: var(--color-primary);
  color: white;
  border-radius: 50%;
  font-size: var(--font-size-xl);
  font-weight: 700;
}

.pain-point-item .content {
  flex: 1;
}

.pain-point-item .title {
  font-size: var(--font-size-lg);
  font-weight: 600;
  color: var(--text-primary);
  margin-bottom: var(--spacing-xs);
}

.pain-point-item .stats {
  display: flex;
  gap: var(--spacing-md);
  margin-bottom: var(--spacing-sm);
  font-size: var(--font-size-sm);
  color: var(--text-muted);
}

.pain-point-item .quote {
  font-size: var(--font-size-sm);
  color: var(--text-secondary);
  font-style: italic;
  padding-left: var(--spacing-md);
  border-left: 2px solid var(--color-warning);
}

/* ========== 竞争对手表格 ========== */
.competitor-table-container {
  padding: var(--spacing-lg);
  background-color: var(--bg-secondary);
  border-radius: var(--border-radius-lg);
  border: 1px solid var(--border-color);
}

.competitor-table {
  width: 100%;
  border-collapse: collapse;
}

.competitor-table th,
.competitor-table td {
  padding: var(--spacing-md);
  text-align: left;
  border-bottom: 1px solid var(--border-color);
}

.competitor-table th {
  font-weight: 600;
  color: var(--text-secondary);
  font-size: var(--font-size-sm);
}

.competitor-table td {
  color: var(--text-primary);
  font-size: var(--font-size-md);
}

.competitor-table tbody tr:hover {
  background-color: rgba(59, 130, 246, 0.05);
}

/* ========== 资源评估 ========== */
.resource-score-card {
  display: flex;
  align-items: center;
  gap: var(--spacing-xl);
  padding: var(--spacing-xl);
  background: linear-gradient(135deg, rgba(34, 197, 94, 0.1), transparent);
  border-radius: var(--border-radius-lg);
  border: 1px solid var(--color-success);
  margin-bottom: var(--spacing-xl);
}

.score-circle {
  flex-shrink: 0;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  width: 120px;
  height: 120px;
  background-color: var(--color-success);
  border-radius: 50%;
  color: white;
}

.score-value {
  font-size: 48px;
  font-weight: 700;
}

.score-label {
  font-size: var(--font-size-sm);
}

.score-verdict {
  flex: 1;
}

.verdict-icon {
  font-size: 32px;
  margin-right: var(--spacing-sm);
}

.verdict-text {
  font-size: var(--font-size-xxl);
  font-weight: 700;
  color: var(--color-success);
}

.verdict-desc {
  margin-top: var(--spacing-sm);
  font-size: var(--font-size-md);
  color: var(--text-secondary);
}

/* ========== 资源维度 ========== */
.resource-dimensions {
  display: flex;
  flex-direction: column;
  gap: var(--spacing-lg);
}

.dimension-item {
  padding: var(--spacing-lg);
  background-color: var(--bg-secondary);
  border-radius: var(--border-radius-lg);
  border: 1px solid var(--border-color);
}

.dimension-header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  margin-bottom: var(--spacing-sm);
}

.dimension-name {
  font-size: var(--font-size-lg);
  font-weight: 600;
  color: var(--text-primary);
}

.dimension-score {
  font-size: var(--font-size-lg);
  font-weight: 700;
  color: var(--color-primary);
}

.progress-bar {
  width: 100%;
  height: 8px;
  background-color: var(--bg-primary);
  border-radius: var(--border-radius-sm);
  overflow: hidden;
  margin-bottom: var(--spacing-sm);
}

.progress-fill {
  height: 100%;
  background: linear-gradient(90deg, var(--color-primary), var(--color-success));
  border-radius: var(--border-radius-sm);
  transition: width 0.3s ease;
}

.dimension-detail {
  font-size: var(--font-size-sm);
  color: var(--text-secondary);
}

/* ========== 响应式 ========== */
@media (max-width: 768px) {
  .step2-header {
    flex-direction: column;
    gap: var(--spacing-md);
  }

  .category-selector select {
    min-width: 100%;
  }

  .chart-row {
    grid-template-columns: 1fr;
  }

  .persona-grid,
  .subcategory-grid {
    grid-template-columns: 1fr;
  }
}
```

#### 交付物清单

- ✅ [modules/step2.html](modules/step2.html) - 五看洞察HTML结构
- ✅ `assets/js/step2.js` - 五看洞察主逻辑
- ✅ `assets/js/step2-data.js` - Mock数据(宠物用品)
- ✅ `assets/js/step2-charts.js` - ECharts配置
- ✅ `assets/css/step2.css` - 五看洞察样式

#### 测试清单

- [ ] 品类选择器正确加载候选池数据
- [ ] 五个Tab切换正常(看行业/市场/客户/竞争/自己)
- [ ] 看行业: 饼图和折线图正确渲染
- [ ] 看市场: 子品类卡片和雷达图正确渲染
- [ ] 看客户: 3个画像和5个痛点正确渲染
- [ ] 看竞争: 竞争雷达图和表格正确渲染
- [ ] 看自己: 资源评估得分和维度正确渲染
- [ ] 导出功能正常(JSON格式)
- [ ] 响应式布局在移动端正常

#### 预估工作量
**2天** (包含调试和优化)

---

### **Phase 2: 市场地图 & STP聚焦** (优先级⭐⭐⭐⭐)

#### 实现目标
实现数字化市场地图和STP战略聚焦分析,帮助用户定位目标细分市场。

#### 界面结构

```html
<!-- 在子模块Tab中添加"market-stp"模块 -->
<div id="market-stp-module" class="sub-module-content">

  <!-- 左右分栏布局 -->
  <div class="market-stp-layout">

    <!-- 左侧: 数字化市场地图 (50%) -->
    <div class="market-map-section">
      <h3>数字化市场地图</h3>

      <!-- 买家vs用户决策流 -->
      <div class="decision-flow-card">
        <h4>决策流分析: 买家 vs 使用者</h4>
        <div class="flow-diagram">
          <!-- SVG或Canvas绘制流程图 -->
        </div>
      </div>

      <!-- 渠道对比 -->
      <div class="channel-comparison">
        <h4>渠道对比: Amazon vs TikTok Shop</h4>
        <div class="channel-cards">
          <div class="channel-card amazon">
            <h5>Amazon</h5>
            <div class="channel-metrics">
              <div class="metric">
                <span class="label">流量类型</span>
                <span class="value">搜索驱动</span>
              </div>
              <div class="metric">
                <span class="label">ROI</span>
                <span class="value">8.0x</span>
              </div>
              <div class="metric">
                <span class="label">转化周期</span>
                <span class="value">3-5天</span>
              </div>
            </div>
          </div>

          <div class="channel-card tiktok">
            <h5>TikTok Shop</h5>
            <div class="channel-metrics">
              <div class="metric">
                <span class="label">流量类型</span>
                <span class="value">发现驱动</span>
              </div>
              <div class="metric">
                <span class="label">ROI</span>
                <span class="value">11.7x</span>
              </div>
              <div class="metric">
                <span class="label">转化周期</span>
                <span class="value">即时</span>
              </div>
            </div>
          </div>
        </div>
      </div>

      <!-- 竞争层次图 -->
      <div class="competition-layers">
        <h4>竞争层次分析</h4>
        <div id="competition-sankey-chart" style="height: 300px;"></div>
      </div>
    </div>

    <!-- 右侧: STP战略聚焦 (50%) -->
    <div class="stp-section">
      <h3>STP战略聚焦</h3>

      <!-- 细分市场评分表 -->
      <div class="segment-scoring-table">
        <h4>细分市场评估 (5个子市场)</h4>
        <table class="segment-table">
          <thead>
            <tr>
              <th>细分市场</th>
              <th>市场规模</th>
              <th>增长潜力</th>
              <th>竞争强度</th>
              <th>综合得分</th>
              <th>操作</th>
            </tr>
          </thead>
          <tbody id="segment-table-body">
            <!-- 动态填充 -->
          </tbody>
        </table>
      </div>

      <!-- 目标市场选择 -->
      <div class="target-market-card selected">
        <div class="market-badge">🎯 目标市场</div>
        <h4>智力训练型</h4>
        <div class="market-score">79.8 分</div>
        <div class="market-highlights">
          <span class="tag">31.2% CAGR</span>
          <span class="tag">$640M GMV</span>
          <span class="tag">竞争分散</span>
        </div>
        <div class="market-positioning">
          <strong>定位策略:</strong> 现代化KONG替代品
        </div>
      </div>

      <!-- 品类角色选择 -->
      <div class="category-role-selector">
        <h4>品类角色</h4>
        <div class="role-options">
          <div class="role-option">
            <input type="radio" name="category-role" value="destination" id="role-destination">
            <label for="role-destination">
              <strong>Destination</strong>
              <span>目标品类 (6-12月盈亏平衡)</span>
            </label>
          </div>
          <div class="role-option selected">
            <input type="radio" name="category-role" value="routine" id="role-routine" checked>
            <label for="role-routine">
              <strong>Routine</strong>
              <span>常规品类 (4-6月盈亏平衡)</span>
            </label>
          </div>
          <div class="role-option">
            <input type="radio" name="category-role" value="seasonal" id="role-seasonal">
            <label for="role-seasonal">
              <strong>Seasonal</strong>
              <span>季节品类 (节日冲刺)</span>
            </label>
          </div>
          <div class="role-option">
            <input type="radio" name="category-role" value="convenience" id="role-convenience">
            <label for="role-convenience">
              <strong>Convenience</strong>
              <span>便捷品类 (快速周转)</span>
            </label>
          </div>
        </div>
      </div>

      <!-- 用户画像 -->
      <div class="target-persona-card">
        <h4>目标用户画像</h4>
        <div class="persona-detail">
          <div class="persona-avatar-large">👩‍💼</div>
          <div class="persona-info">
            <h5>Emily</h5>
            <p class="persona-basic">29岁 | 产品经理 | 旧金山</p>
            <p class="persona-income">家庭年收入: $95K</p>
            <div class="persona-pet">
              <strong>宠物:</strong> Bella (2岁金毛)
            </div>
            <div class="persona-pain">
              <strong>核心痛点:</strong> KONG太难清洁,狗狗很快失去兴趣
            </div>
            <div class="persona-expectation">
              <strong>期望:</strong> 可机洗 + 可调难度 + 高颜值
            </div>
          </div>
        </div>
      </div>
    </div>

  </div>
</div>
```

#### JavaScript实现

```javascript
// assets/js/step2.js (Phase 2 新增)

Step2.modules.marketStp = {

  init() {
    const data = Step2Data.getMarketSTPData(Step2.state.currentCategory);
    this.renderMarketMap(data.marketMap);
    this.renderSTPAnalysis(data.stp);
    this.bindEvents();
  },

  renderMarketMap(data) {
    // 渲染渠道对比
    // 渲染竞争层次图 (Sankey)
  },

  renderSTPAnalysis(data) {
    // 渲染细分市场评分表
    const tbody = document.getElementById('segment-table-body');
    tbody.innerHTML = data.segments.map(seg => `
      <tr ${seg.isSelected ? 'class="selected"' : ''}>
        <td><strong>${seg.name}</strong></td>
        <td>${seg.marketSize}</td>
        <td><span class="score ${seg.growthScore > 80 ? 'high' : ''}">${seg.growthScore}</span></td>
        <td><span class="score">${seg.competitionScore}</span></td>
        <td><strong class="score-total">${seg.totalScore}</strong></td>
        <td>
          <button class="btn btn-sm ${seg.isSelected ? 'btn-primary' : 'btn-outline'}"
                  data-segment="${seg.id}">
            ${seg.isSelected ? '已选择' : '选择'}
          </button>
        </td>
      </tr>
    `).join('');

    // 渲染目标市场卡片
    // 渲染品类角色选择
    // 渲染用户画像
  },

  bindEvents() {
    // 监听细分市场选择
    // 监听品类角色切换
  }
};
```

#### 交付物清单

- ✅ [modules/step2.html](modules/step2.html) - 新增 market-stp 模块
- ✅ `assets/js/step2.js` - 新增 marketStp 模块
- ✅ `assets/js/step2-data.js` - 新增 getMarketSTPData()
- ✅ `assets/css/step2.css` - 新增市场地图和STP样式

#### 预估工作量
**1.5天**

---

### **Phase 3: SPAN矩阵 & $APPEALS分析** (优先级⭐⭐⭐⭐)

#### 实现目标
实现SPAN投资决策矩阵和$APPEALS差异化分析,给出量化的竞争定位建议。

#### 界面结构

```html
<div id="span-appeals-module" class="sub-module-content">

  <!-- 上半部分: SPAN矩阵 -->
  <div class="span-section">
    <h3>SPAN矩阵智能评估</h3>

    <div class="span-layout">
      <!-- 左侧: 矩阵图 -->
      <div class="span-matrix-container">
        <div id="span-scatter-chart" style="height: 500px;"></div>
      </div>

      <!-- 右侧: 评分详情 -->
      <div class="span-details">
        <div class="span-result-card star">
          <div class="result-badge">⭐ 明星象限</div>
          <div class="result-position">(83, 83)</div>
          <div class="result-verdict">
            <div class="verdict-label">AI决策</div>
            <div class="verdict-value approved">APPROVED</div>
            <div class="verdict-score">83/100</div>
          </div>
          <div class="result-strategy">
            <strong>投资策略:</strong> 激进投资
          </div>
        </div>

        <!-- Y轴评分详情 -->
        <div class="axis-score-card">
          <h5>Y轴: 市场吸引力</h5>
          <div class="axis-score">83.15/100</div>
          <div class="dimension-scores">
            <div class="dimension">
              <span class="name">市场规模 (30%)</span>
              <span class="score">82</span>
            </div>
            <div class="dimension">
              <span class="name">增长率 (30%)</span>
              <span class="score high">95</span>
            </div>
            <!-- 其他维度... -->
          </div>
        </div>

        <!-- X轴评分详情 -->
        <div class="axis-score-card">
          <h5>X轴: 竞争地位 (易进入性)</h5>
          <div class="axis-score">82.65/100</div>
          <div class="dimension-scores">
            <div class="dimension">
              <span class="name">CR3集中度 (35%)</span>
              <span class="score high">85</span>
            </div>
            <div class="dimension">
              <span class="name">LQS质量差距 (30%)</span>
              <span class="score high">88</span>
            </div>
            <!-- 其他维度... -->
          </div>
        </div>
      </div>
    </div>
  </div>

  <!-- 下半部分: $APPEALS分析 -->
  <div class="appeals-section">
    <h3>$APPEALS差异化分析</h3>

    <div class="appeals-layout">
      <!-- 左侧: 雷达图 -->
      <div class="appeals-radar-container">
        <h4>8维竞争对比</h4>
        <div id="appeals-radar-chart" style="height: 450px;"></div>
      </div>

      <!-- 右侧: 差距分析 -->
      <div class="appeals-gap-analysis">
        <h4>差距策略</h4>

        <!-- 开放差距 (建立优势) -->
        <div class="gap-category open-gap">
          <h5>🚀 开放差距 (建立优势)</h5>
          <div class="gap-items">
            <div class="gap-item">
              <div class="gap-dimension">Social Acceptance</div>
              <div class="gap-score">9.5 vs 6.2 (+3.3)</div>
              <div class="gap-strategy">
                5种马卡龙配色 + 北欧设计 + UGC激励
              </div>
            </div>
            <div class="gap-item">
              <div class="gap-dimension">Ease of Use</div>
              <div class="gap-score">9.5 vs 7.0 (+2.5)</div>
              <div class="gap-strategy">
                一体成型可机洗 (vs KONG手洗缝隙)
              </div>
            </div>
            <!-- 其他开放差距... -->
          </div>
        </div>

        <!-- 填平差距 (补齐短板) -->
        <div class="gap-category close-gap">
          <h5>🔧 填平差距 (补齐短板)</h5>
          <div class="gap-items">
            <div class="gap-item">
              <div class="gap-dimension">Performance</div>
              <div class="gap-score">9.0 vs 9.5 (-0.5)</div>
              <div class="gap-strategy">
                天然橡胶+TPR, 通过50磅咬合力测试
              </div>
            </div>
            <!-- 其他填平差距... -->
          </div>
        </div>

        <!-- 成本影响 -->
        <div class="cost-impact-card">
          <h5>成本影响</h5>
          <div class="cost-breakdown">
            <div class="cost-item">
              <span class="label">初始COGS</span>
              <span class="value">$8.55</span>
            </div>
            <div class="cost-item optimized">
              <span class="label">KANO优化后</span>
              <span class="value">$7.50</span>
            </div>
            <div class="cost-savings">
              节省 $1.05 (12.3%)
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>

</div>
```

#### JavaScript实现

```javascript
// assets/js/step2.js (Phase 3 新增)

Step2.modules.spanAppeals = {

  init() {
    const data = Step2Data.getSPANAppealsData(Step2.state.currentCategory);
    this.renderSPAN(data.span);
    this.renderAPPEALS(data.appeals);
  },

  renderSPAN(data) {
    // SPAN散点图
    const chart = echarts.init(document.getElementById('span-scatter-chart'));
    chart.setOption(Step2Charts.getSPANScatterOption(data));

    // 渲染评分详情
  },

  renderAPPEALS(data) {
    // APPEALS雷达图
    const chart = echarts.init(document.getElementById('appeals-radar-chart'));
    chart.setOption(Step2Charts.getAPPEALSRadarOption(data));

    // 渲染差距分析
  }
};
```

#### 交付物清单

- ✅ [modules/step2.html](modules/step2.html) - 新增 span-appeals 模块
- ✅ `assets/js/step2.js` - 新增 spanAppeals 模块
- ✅ `assets/js/step2-data.js` - 新增 getSPANAppealsData()
- ✅ `assets/js/mcda.js` - 新增 calculateSPAN() 和 calculateAPPEALS()
- ✅ `assets/css/step2.css` - 新增SPAN和APPEALS样式

#### 预估工作量
**1.5天**

---

### **Phase 4: KANO需求分级引擎** (优先级⭐⭐⭐)

#### 实现目标
实现KANO模型特征分级,优化MVP特征集和成本结构。

#### 界面结构

```html
<div id="kano-module" class="sub-module-content">
  <h3>KANO需求分级引擎</h3>

  <!-- 顶部: 分类统计 -->
  <div class="kano-summary">
    <div class="kano-category-card must-be">
      <div class="category-name">Must-be (基本型)</div>
      <div class="category-count">6 个特征</div>
      <div class="category-cost">$3.90 (52%)</div>
    </div>
    <div class="kano-category-card one-dimensional">
      <div class="category-name">One-dimensional (期望型)</div>
      <div class="category-count">4 个特征</div>
      <div class="category-cost">$1.35 (18%)</div>
    </div>
    <div class="kano-category-card attractive">
      <div class="category-name">Attractive (兴奋型)</div>
      <div class="category-count">4 个特征</div>
      <div class="category-cost">$2.25 (30%)</div>
    </div>
    <div class="kano-category-card indifferent">
      <div class="category-name">Indifferent (无差异)</div>
      <div class="category-count deleted">3 个特征 (已删除)</div>
      <div class="category-cost saved">节省 $3.88</div>
    </div>
  </div>

  <!-- 中间: 特征列表 (可拖拽) -->
  <div class="kano-features-list">
    <div class="kano-category-section" data-category="must-be">
      <h4>Must-be (基本型)</h4>
      <div class="feature-cards" id="must-be-features">
        <!-- 特征卡片 -->
        <div class="feature-card" draggable="true" data-feature-id="F1">
          <div class="feature-header">
            <span class="feature-id">F1</span>
            <span class="feature-cost">$0.85</span>
          </div>
          <div class="feature-name">天然橡胶材质</div>
          <div class="feature-stats">
            <span>提及率: 78%</span>
            <span>满意影响: +45%</span>
          </div>
          <div class="feature-actions">
            <button class="btn-icon" title="编辑">✏️</button>
            <button class="btn-icon" title="删除">🗑️</button>
          </div>
        </div>
        <!-- 其他特征... -->
      </div>
    </div>

    <!-- 其他KANO分类... -->
  </div>

  <!-- 底部: MVP总结 -->
  <div class="mvp-summary-card">
    <h4>MVP V1.0 总结</h4>
    <div class="mvp-metrics">
      <div class="metric">
        <span class="label">保留特征</span>
        <span class="value">14 个</span>
      </div>
      <div class="metric">
        <span class="label">删除特征</span>
        <span class="value">3 个</span>
      </div>
      <div class="metric highlight">
        <span class="label">最终COGS</span>
        <span class="value">$7.50</span>
      </div>
      <div class="metric">
        <span class="label">成本优化</span>
        <span class="value saved">-$1.05 (12.3%)</span>
      </div>
    </div>
    <div class="mvp-recommendations">
      <h5>优化建议</h5>
      <ul>
        <li>✅ 保留所有Must-be特征,确保基本满意度</li>
        <li>✅ Attractive特征作为差异化亮点,保持竞争优势</li>
        <li>⚠️ F21训练小册子简化为4页折页 ($0.25→$0.10)</li>
        <li>⚠️ F24质保策略调整: 全额退款→6个月免费换新 ($1.25→$0.45)</li>
      </ul>
    </div>
  </div>

</div>
```

#### JavaScript实现

```javascript
// assets/js/step2.js (Phase 4 新增)

Step2.modules.kano = {

  init() {
    const data = Step2Data.getKANOData(Step2.state.currentCategory);
    this.renderKANOCategories(data);
    this.renderFeatureCards(data.features);
    this.renderMVPSummary(data);
    this.initDragDrop();
  },

  renderKANOCategories(data) {
    // 渲染顶部统计卡片
  },

  renderFeatureCards(features) {
    // 渲染特征卡片列表
  },

  initDragDrop() {
    // 实现拖拽功能(可选)
  },

  deleteFeature(featureId) {
    // 删除特征并重新计算COGS
  },

  recalculateCOGS() {
    // 重新计算总成本
  }
};
```

#### 交付物清单

- ✅ [modules/step2.html](modules/step2.html) - 新增 kano 模块
- ✅ `assets/js/step2.js` - 新增 kano 模块
- ✅ `assets/js/step2-data.js` - 新增 getKANOData()
- ✅ `assets/css/step2.css` - 新增KANO样式

#### 预估工作量
**1天**

---

### **Phase 5: 数字化Charter生成器** (优先级⭐⭐⭐)

#### 实现目标
整合前面所有分析数据,生成完整的产品开发任务书(Charter)。

#### 界面结构

```html
<div id="charter-module" class="sub-module-content">
  <h3>数字化Charter生成器</h3>

  <div class="charter-layout">

    <!-- 左侧: Charter预览 (PDF样式) -->
    <div class="charter-preview" id="charter-preview-content">

      <!-- 封面 -->
      <div class="charter-cover">
        <h1>产品开发任务书</h1>
        <h2>PawGenius™ Smart Interactive Dog Toy</h2>
        <div class="charter-meta">
          <div>版本: V1.0</div>
          <div>日期: 2025-11-19</div>
          <div>状态: <span class="badge approved">已批准</span></div>
        </div>
      </div>

      <!-- 1. Executive Summary -->
      <div class="charter-section">
        <h3>1. Executive Summary</h3>
        <div class="summary-grid">
          <div class="summary-item">
            <span class="label">目标市场</span>
            <span class="value">智力训练型宠物玩具 ($640M GMV)</span>
          </div>
          <div class="summary-item">
            <span class="label">市场定位</span>
            <span class="value">现代化KONG替代品</span>
          </div>
          <div class="summary-item">
            <span class="label">目标价格</span>
            <span class="value">$24.99 (Medium)</span>
          </div>
          <div class="summary-item highlight">
            <span class="label">12月预期ROI</span>
            <span class="value">114% ($28.5K净利)</span>
          </div>
        </div>
      </div>

      <!-- 2-9章节... -->
      <!-- (从设计文档2.7提取完整内容) -->

    </div>

    <!-- 右侧: 操作区 -->
    <div class="charter-actions-panel">

      <!-- AI批准决策 -->
      <div class="ai-decision-card approved">
        <div class="decision-icon">✅</div>
        <div class="decision-verdict">APPROVED</div>
        <div class="decision-score">83/100</div>
        <div class="decision-reason">
          市场吸引力高 (83分) + 竞争地位优 (83分) = 明星象限
        </div>
      </div>

      <!-- 操作按钮 -->
      <div class="charter-actions">
        <button class="btn btn-outline btn-block" id="edit-charter-btn">
          ✏️ 编辑Charter
        </button>
        <button class="btn btn-primary btn-block" id="export-charter-pdf-btn">
          📥 导出PDF
        </button>
        <button class="btn btn-success btn-block" id="submit-charter-btn">
          ✅ 提交审批 (进入Step 3)
        </button>
      </div>

      <!-- 下一步指引 -->
      <div class="next-steps-card">
        <h5>下一步</h5>
        <ul>
          <li>✅ Step 1: 机会发现 (已完成)</li>
          <li>✅ Step 2: 品类洞察 (已完成)</li>
          <li>⏭️ Step 3: 产品选择 (待进入)</li>
        </ul>
      </div>

    </div>

  </div>
</div>
```

#### JavaScript实现

```javascript
// assets/js/step2.js (Phase 5 新增)

Step2.modules.charter = {

  init() {
    const charter = this.generateCharter();
    this.renderCharterPreview(charter);
    this.bindEvents();
  },

  generateCharter() {
    // 整合所有模块数据
    return {
      wukan: Step2.state.wukanData,
      stp: Step2.state.stpData,
      span: Step2.state.spanData,
      appeals: Step2.state.appealsData,
      kano: Step2.state.kanoData,
      financial: this.calculateFinancials(),
      risks: this.getRiskAnalysis(),
      gtm: this.getGTMStrategy()
    };
  },

  renderCharterPreview(charter) {
    // 渲染9个章节
  },

  calculateFinancials() {
    // 12月P&L预测
    return {
      initialInvestment: 25000,
      firstOrderQty: 1500,
      breakEvenMonth: 5,
      breakEvenUnits: 2309,
      gmv12Month: 152250,
      netProfit12Month: 28519,
      roi: 1.14
    };
  },

  exportCharterPDF() {
    // 使用html2pdf.js导出
    const element = document.getElementById('charter-preview-content');
    html2pdf()
      .from(element)
      .set({
        margin: 1,
        filename: `Charter_PawGenius_${Date.now()}.pdf`,
        html2canvas: { scale: 2 },
        jsPDF: { unit: 'in', format: 'a4', orientation: 'portrait' }
      })
      .save();
  },

  submitCharter() {
    // 保存Charter到候选池
    AppStorage.saveCharter(this.generateCharter());

    // 跳转到Step 3
    loadStepModule(3);
  }
};
```

#### 交付物清单

- ✅ [modules/step2.html](modules/step2.html) - 新增 charter 模块
- ✅ `assets/js/step2.js` - 新增 charter 模块
- ✅ `assets/js/step2-data.js` - 新增财务计算、风险分析、GTM策略
- ✅ `assets/css/step2.css` - 新增Charter样式(PDF风格)

#### 预估工作量
**1天**

---

## 📊 数据Mock策略

### 数据来源

所有Mock数据直接从设计文档提取:

```
2.1-五看洞察仪表板设计.md → Step2Data.getWuKanData()
2.2-数字化市场地图设计.md → Step2Data.getMarketMapData()
2.3-STP战略聚焦设计.md → Step2Data.getSTPData()
2.4-SPAN矩阵智能评估设计.md → Step2Data.getSPANData()
2.5-APPEALS差异化分析设计.md → Step2Data.getAPPEALSData()
2.6-KANO需求分级引擎设计.md → Step2Data.getKANOData()
2.7-数字化Charter生成器设计.md → Step2Data.getCharterData()
```

### 数据结构示例

```javascript
// step2-data.js
window.Step2Data = {

  // 主数据入口
  getAllData(category) {
    return {
      wukan: this.getWuKanData(category),
      marketMap: this.getMarketMapData(category),
      stp: this.getSTPData(category),
      span: this.getSPANData(category),
      appeals: this.getAPPEALSData(category),
      kano: this.getKANOData(category)
    };
  },

  // 每个函数返回对应模块的完整数据
  getWuKanData(category) { /* ... */ },
  getMarketMapData(category) { /* ... */ },
  // ...
};
```

### 多品类支持

初期只实现**宠物用品**的完整数据,其他品类返回默认值:

```javascript
getWuKanData(category) {
  const dataMap = {
    "宠物用品": { /* 完整数据 */ },
    "家居装饰": { /* 可选:后续补充 */ }
  };
  return dataMap[category] || dataMap["宠物用品"];
}
```

---

## 📐 开发规范

### 代码规范

1. **命名约定**
   - 文件名: kebab-case (`step2-data.js`)
   - 函数名: camelCase (`renderWuKanTab()`)
   - 常量: UPPER_SNAKE_CASE (`MOCK_DATA`)
   - CSS类: kebab-case (`.wukan-panel`)

2. **注释规范**
   ```javascript
   /**
    * 函数说明
    * @param {Type} paramName - 参数说明
    * @returns {Type} 返回值说明
    */
   ```

3. **ECharts图表初始化**
   ```javascript
   // ✅ 推荐: 检查DOM是否存在
   const chartDom = document.getElementById('chart-id');
   if (!chartDom) return;
   const chart = echarts.init(chartDom);
   ```

4. **事件绑定**
   ```javascript
   // ✅ 推荐: 使用事件委托
   container.addEventListener('click', (e) => {
     if (e.target.matches('.btn-delete')) {
       // 处理删除
     }
   });
   ```

### CSS规范

1. **使用CSS变量** (已在`variables.css`定义)
   ```css
   color: var(--text-primary);
   background-color: var(--bg-secondary);
   padding: var(--spacing-md);
   ```

2. **响应式断点**
   ```css
   @media (max-width: 768px) { /* 移动端 */ }
   @media (max-width: 1200px) { /* 平板 */ }
   ```

3. **深色主题一致性**
   - 背景色: `--bg-primary` (#18181b), `--bg-secondary` (#27272a)
   - 文本色: `--text-primary` (#e4e4e7), `--text-secondary` (#a1a1aa)
   - 边框色: `--border-color` (#3f3f46)

### Git提交规范

```
feat(step2): 实现五看洞察仪表板
fix(step2): 修复雷达图渲染问题
style(step2): 优化KANO特征卡片样式
docs(step2): 更新实施计划文档
```

---

## 🧪 测试计划

### 单元测试清单

#### Phase 1测试
- [ ] 品类选择器加载候选池数据
- [ ] 五看Tab切换正常
- [ ] 所有ECharts图表正确渲染
- [ ] 数据更新后UI同步刷新
- [ ] 导出功能正常

#### Phase 2测试
- [ ] 市场地图渠道对比显示正确
- [ ] STP细分市场评分计算正确
- [ ] 目标市场选择交互正常
- [ ] 品类角色切换正常

#### Phase 3测试
- [ ] SPAN矩阵坐标计算正确
- [ ] 象限判断逻辑正确
- [ ] APPEALS雷达图对比正确
- [ ] 差距分析数据准确

#### Phase 4测试
- [ ] KANO分类统计正确
- [ ] 特征删除后COGS重新计算
- [ ] MVP总结数据准确

#### Phase 5测试
- [ ] Charter内容完整整合所有模块数据
- [ ] PDF导出功能正常
- [ ] 提交后正确跳转Step 3

### 集成测试

- [ ] Step 1 → Step 2 数据传递正确
- [ ] Step 2 各子模块之间数据联动
- [ ] Step 2 → Step 3 Charter传递正确
- [ ] 候选池与Step 2交互正常

### 兼容性测试

- [ ] Chrome 120+
- [ ] Safari 17+
- [ ] Firefox 120+
- [ ] 移动端响应式布局

---

## ⚠️ 风险控制

### 技术风险

| 风险 | 概率 | 影响 | 应对策略 |
|------|------|------|----------|
| ECharts图表在深色主题下渲染异常 | 中 | 中 | 使用统一的深色主题配置,提前测试 |
| 大数据量导致页面卡顿 | 低 | 中 | 数据分页,图表按需渲染 |
| PDF导出样式丢失 | 中 | 低 | 使用html2pdf.js,提前测试 |
| 跨浏览器兼容性问题 | 低 | 低 | 使用现代浏览器标准API |

### 进度风险

| 风险 | 概率 | 影响 | 应对策略 |
|------|------|------|----------|
| 某个Phase开发时间超预期 | 中 | 中 | 优先保证核心功能,非核心功能可延后 |
| 设计变更导致返工 | 低 | 高 | 每个Phase完成后立即Review |

### 质量风险

| 风险 | 概率 | 影响 | 应对策略 |
|------|------|------|----------|
| Mock数据与设计文档不一致 | 低 | 中 | 开发前仔细核对设计文档 |
| UI与设计文档描述不符 | 中 | 低 | 参考Step 1已有样式,保持一致性 |

---

## 📅 开发时间线

```
Day 1-2:   Phase 1 (五看洞察)
Day 3:     Phase 2 (市场地图 & STP)
Day 4:     Phase 3 (SPAN & APPEALS) - Part 1
Day 5:     Phase 3 (SPAN & APPEALS) - Part 2
Day 6:     Phase 4 (KANO分级)
Day 7:     Phase 5 (Charter生成)
Day 8:     集成测试 & Bug修复
Day 9:     优化 & 文档整理
Day 10:    缓冲时间

总计: 7-10天
```

---

## ✅ 验收标准

### 功能验收

- [x] 所有7个子模块(2.1-2.7)功能完整实现
- [x] 数据Mock覆盖宠物用品品类完整场景
- [x] 所有ECharts图表正确渲染
- [x] 与Step 1数据传递正常
- [x] Charter生成功能完整
- [x] PDF导出功能正常

### 性能验收

- [x] 页面首次加载时间 < 2秒
- [x] Tab切换响应时间 < 300ms
- [x] 图表渲染时间 < 500ms

### 视觉验收

- [x] 深色主题一致性
- [x] 响应式布局在移动端正常
- [x] 与Step 1视觉风格统一

---

## 📚 参考资料

- 设计文档: `docs/design/从品类到产品的战略决策/2.1-2.7.md`
- Step 1 实现: `modules/step1.html`, `assets/js/step1.js`
- ECharts文档: https://echarts.apache.org/
- html2pdf.js文档: https://github.com/eKoopmans/html2pdf.js

---

## 🎯 下一步行动

1. **用户审阅本计划文档** ← 当前阶段
2. 确认后开始 **Phase 1: 五看洞察仪表板** 开发
3. 每个Phase完成后进行代码Review
4. 最终集成测试并交付

---

**文档结束** | 等待用户审阅批准后开始开发 🚀
