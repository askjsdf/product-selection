# AI选品助手 - 前端架构设计与开发规划

> **文档性质**: 🔒 宪法级架构文档（穿越周期、穿越对话窗口的唯一指导文件）
> **文档版本**: V1.0
> **最后更新**: 2025-11-18
> **维护原则**: 所有后续 AI 开发必须严格遵循本文档，不得擅自修改架构

---

## ⚠️ 重要声明

本文档是整个项目的**架构宪法**，具有以下特性：

1. **唯一性**: 所有后续开发对话必须携带本文档 + 对应 Step 产品设计文档
2. **权威性**: 任何架构级修改必须更新本文档版本号
3. **完整性**: 包含全景架构 + 超详细 200+ 条 TODO 列表
4. **延续性**: 后续 AI 只负责执行 TODO，不做全景理解

---

## 📐 第一部分：整体架构设计（全景图）

### 1.1 架构理念

**核心原则**：主框架 + 模块化 Tab 页

```
┌─────────────────────────────────────────────────────────┐
│                    index.html (主框架)                   │
│  ┌─────────────────────────────────────────────────┐   │
│  │  全局导航 + 候选池 + 6个Tab切换                   │   │
│  └─────────────────────────────────────────────────┘   │
│                                                          │
│  ┌──────────┬──────────┬──────────┬──────────┬────┐   │
│  │ Step 1   │ Step 2   │ Step 3   │ Step 4   │... │   │
│  │ 机会发现  │ 品类洞察  │ 产品选择  │ 利润分析  │    │   │
│  └──────────┴──────────┴──────────┴──────────┴────┘   │
│         ↓          ↓          ↓          ↓              │
│  ┌──────────┬──────────┬──────────┬──────────┬────┐   │
│  │step1.html│step2.html│step3.html│step4.html│... │   │
│  │step1.js  │step2.js  │step3.js  │step4.js  │    │   │
│  │step1.css │step2.css │step3.css │step4.css │    │   │
│  └──────────┴──────────┴──────────┴──────────┴────┘   │
│                                                          │
│  共享层：utils.js + mcda.js + common.css               │
└─────────────────────────────────────────────────────────┘
```

**设计优势**：
- ✅ 模块完全独立，修改 Step 3 不影响 Step 1
- ✅ 代码可读性强，每个文件只关注单一功能
- ✅ 团队协作友好，多人可并行开发不同 Step
- ✅ 按需加载，用户切换 Tab 时才加载对应模块
- ✅ 易于调试，问题定位精准

---

### 1.2 技术栈选型

```yaml
核心技术:
  - 前端框架: 原生 HTML + CSS + JavaScript (ES6+)
  - UI框架: Bootstrap 5.3.x (响应式布局)
  - 图表库: ECharts 5.5.x (所有可视化)
  - 工具库: Axios (数据加载) + Day.js (日期处理)
  - PDF导出: html2pdf.js

数据层:
  - 样例数据: JSON 文件 (静态托管)
  - 数据格式: 统一 JSON Schema
  - 数据加载: Fetch API / Axios

样式层:
  - 主框架样式: main.css (全局布局、导航、候选池)
  - 公共组件: common.css (按钮、表格、卡片、模态框)
  - 模块样式: step1.css ~ step6.css (各模块专属)
  - 变量系统: CSS Variables (颜色、间距、字体)

脚本层:
  - 主框架逻辑: main.js (Tab切换、候选池管理、全局状态)
  - 工具函数: utils.js (格式化、日期、数字、排序)
  - 计算引擎: mcda.js (MCDA v1.0/v2.0/v3.0评分算法)
  - 模块逻辑: step1.js ~ step6.js (各模块专属功能)

开发工具:
  - 本地服务器: Live Server (VSCode插件) 或 Python http.server
  - 版本管理: Git
  - 代码规范: ESLint (可选)
```

---

### 1.3 项目目录结构（最终形态）

```
product-selection-demo/
├── index.html                          # 主框架页面
├── assets/
│   ├── css/
│   │   ├── variables.css              # CSS变量定义
│   │   ├── reset.css                  # 样式重置
│   │   ├── main.css                   # 主框架样式
│   │   ├── common.css                 # 公共组件样式
│   │   ├── step1.css                  # Step 1专属样式
│   │   ├── step2.css                  # Step 2专属样式
│   │   ├── step3.css                  # Step 3专属样式
│   │   ├── step4.css                  # Step 4专属样式
│   │   ├── step5.css                  # Step 5专属样式
│   │   └── step6.css                  # Step 6专属样式
│   ├── js/
│   │   ├── main.js                    # 主框架逻辑
│   │   ├── utils.js                   # 工具函数库
│   │   ├── mcda.js                    # MCDA计算引擎
│   │   ├── storage.js                 # 候选池存储管理
│   │   ├── step1.js                   # Step 1业务逻辑
│   │   ├── step2.js                   # Step 2业务逻辑
│   │   ├── step3.js                   # Step 3业务逻辑
│   │   ├── step4.js                   # Step 4业务逻辑
│   │   ├── step5.js                   # Step 5业务逻辑
│   │   └── step6.js                   # Step 6业务逻辑
│   ├── data/
│   │   ├── combinations.json          # 480组合数据 (精简30条)
│   │   ├── ceiling-analysis.json      # 行业天花板数据 (5个品类)
│   │   ├── products.json              # 产品列表数据 (50条)
│   │   ├── category-insights.json     # 品类洞察数据
│   │   ├── competitors.json           # 竞品数据
│   │   ├── profit-scenarios.json      # 利润场景数据
│   │   ├── reviews-qa.json            # 评论&QA数据
│   │   └── strategies.json            # 策略模板数据
│   ├── images/
│   │   ├── logo.png                   # 产品Logo
│   │   ├── icons/                     # 图标素材
│   │   └── placeholders/              # 占位图片
│   └── libs/
│       ├── bootstrap.min.css          # Bootstrap 5.3.x
│       ├── bootstrap.bundle.min.js    # Bootstrap JS
│       ├── echarts.min.js             # ECharts 5.5.x
│       ├── axios.min.js               # Axios
│       ├── dayjs.min.js               # Day.js
│       └── html2pdf.bundle.min.js     # html2pdf
├── modules/
│   ├── step1.html                     # Step 1: 机会发现 (独立HTML片段)
│   ├── step2.html                     # Step 2: 品类洞察
│   ├── step3.html                     # Step 3: 产品选择
│   ├── step4.html                     # Step 4: 利润分析
│   ├── step5.html                     # Step 5: 竞品拆解
│   └── step6.html                     # Step 6: 策略决策
├── docs/
│   ├── ARCHITECTURE.md                # 本文档
│   └── design/                        # 产品设计文档
│       ├── 01-整体流程和模块设计.md
│       ├── 02-Step1-机会发现阶段-功能设计.md
│       ├── 03-Step2-市场与品类洞察阶段-功能设计.md
│       ├── 04-Step3-产品选择阶段-功能设计.md
│       ├── 05-Step4-利润分析与可行性验证阶段-功能设计.md
│       ├── 06-Step5-竞品深度拆解阶段-功能设计.md
│       └── 07-Step6-策略决策与执行规划阶段-功能设计.md
└── README.md                           # 项目说明
```

---

### 1.4 核心数据流

```
┌─────────────────────────────────────────────────────────┐
│                     数据流向图                           │
└─────────────────────────────────────────────────────────┘

[用户操作]
    ↓
[main.js] ──→ 切换Tab ──→ loadStep(stepNumber)
    ↓
[动态加载模块]
    ├─→ 加载 modules/stepN.html → 插入 #step-container
    ├─→ 加载 assets/css/stepN.css → 插入 <head>
    └─→ 加载 assets/js/stepN.js → 执行初始化函数
    ↓
[stepN.js]
    ├─→ 调用 utils.js (格式化函数)
    ├─→ 调用 mcda.js (评分计算)
    ├─→ 调用 storage.js (候选池操作)
    ├─→ 加载 assets/data/*.json (样例数据)
    └─→ 渲染 ECharts 图表
    ↓
[用户交互] → 添加到候选池 → storage.js 保存到 localStorage
    ↓
[候选池组件] (main.js管理) → 实时显示
    ↓
[进入下一步] → loadStep(stepNumber + 1) → 传递候选池数据
```

---

### 1.5 模块间通信机制

**全局状态对象** (main.js 维护):

```javascript
window.AppState = {
  currentStep: 1,                    // 当前步骤
  opportunityPool: [],               // 机会候选池 (Step 1 → Step 2)
  productPool: [],                   // 产品候选池 (Step 3 → Step 4)
  selectedProducts: [],              // 精选产品 (Step 4 → Step 5)
  finalDecisions: [],                // 最终决策 (Step 6)
  userPreferences: {                 // 用户偏好 (MCDA权重等)
    weights: { MA: 25, CF: 20, PP: 30, RF: 15, RC: 10 },
    strategyMode: '综合最优'
  }
};
```

**候选池操作 API** (storage.js 提供):

```javascript
// 添加到机会候选池
AppStorage.addToOpportunityPool(combination);

// 从机会候选池移除
AppStorage.removeFromOpportunityPool(combinationId);

// 获取机会候选池
AppStorage.getOpportunityPool();

// 清空机会候选池
AppStorage.clearOpportunityPool();

// 类似的产品候选池 API
AppStorage.addToProductPool(product);
// ...
```

**事件系统** (main.js 实现):

```javascript
// 发布事件
window.dispatchEvent(new CustomEvent('opportunity-added', {
  detail: { combination }
}));

// 订阅事件 (在 stepN.js 中)
window.addEventListener('opportunity-added', (e) => {
  console.log('New opportunity:', e.detail.combination);
  updateUI();
});
```

---

### 1.6 样式规范

**CSS Variables 定义** (variables.css):

```css
:root {
  /* 主色系 */
  --primary-color: #1a73e8;
  --secondary-color: #34a853;
  --accent-color: #fbbc04;
  --danger-color: #ea4335;

  /* 灰度 */
  --gray-50: #f8f9fa;
  --gray-100: #e9ecef;
  --gray-200: #dee2e6;
  --gray-600: #6c757d;
  --gray-900: #212529;

  /* 评分颜色 */
  --score-excellent: #d94e5d;  /* 90-100分 红色 */
  --score-good: #eac736;       /* 80-90分 橙色 */
  --score-fair: #50a3ba;       /* 70-80分 黄色 */
  --score-poor: #adb5bd;       /* <70分 灰色 */

  /* 间距 */
  --spacing-xs: 0.25rem;
  --spacing-sm: 0.5rem;
  --spacing-md: 1rem;
  --spacing-lg: 1.5rem;
  --spacing-xl: 2rem;

  /* 字体 */
  --font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, sans-serif;
  --font-size-xs: 0.75rem;
  --font-size-sm: 0.875rem;
  --font-size-md: 1rem;
  --font-size-lg: 1.25rem;
  --font-size-xl: 1.5rem;

  /* 圆角 */
  --border-radius-sm: 0.25rem;
  --border-radius-md: 0.5rem;
  --border-radius-lg: 0.75rem;

  /* 阴影 */
  --shadow-sm: 0 1px 2px rgba(0,0,0,0.05);
  --shadow-md: 0 4px 6px rgba(0,0,0,0.1);
  --shadow-lg: 0 10px 15px rgba(0,0,0,0.15);
}
```

**BEM 命名规范**:

```css
/* Block */
.heatmap { }

/* Element */
.heatmap__cell { }
.heatmap__tooltip { }

/* Modifier */
.heatmap__cell--excellent { }
.heatmap__cell--good { }
```

---

### 1.7 JavaScript 编码规范

**命名约定**:

```javascript
// 常量：全大写 + 下划线
const MAX_POOL_SIZE = 5;
const API_BASE_URL = '/assets/data';

// 变量：驼峰命名
let currentStep = 1;
let opportunityPool = [];

// 函数：动词开头 + 驼峰
function loadStepModule(stepNumber) { }
function calculateMCDAScore(data) { }
function renderHeatmap(chartData) { }

// 类：帕斯卡命名
class MCDAEngine { }
class StorageManager { }

// 私有变量/函数：下划线前缀
let _cachedData = null;
function _validateInput(data) { }
```

**注释规范**:

```javascript
/**
 * 加载 Step 模块
 * @param {number} stepNumber - 步骤编号 (1-6)
 * @returns {Promise<void>}
 */
async function loadStepModule(stepNumber) {
  // 实现...
}
```

**错误处理**:

```javascript
try {
  const data = await loadData('combinations.json');
  renderHeatmap(data);
} catch (error) {
  console.error('Failed to load combinations:', error);
  showErrorMessage('数据加载失败，请刷新页面重试');
}
```

---

### 1.8 数据 Schema 规范

**组合数据结构** (combinations.json):

```json
{
  "combinations": [
    {
      "id": "combo_001",
      "category": "3C配件",
      "country": "US",
      "platform": "Amazon",
      "mcda_score": 82.5,
      "scores_breakdown": {
        "MA": 85.3,
        "CF": 76.1,
        "PP": 81.2,
        "RF": 75.8,
        "RC": 77.1
      },
      "market_data": {
        "gmv": 58000000,
        "growth_rate": 185,
        "search_volume": 120000,
        "monthly_sales": 850000,
        "seller_count": 8520
      },
      "competition_data": {
        "cr5": 35.2,
        "top1_share": 8.5,
        "monopoly_index": 2.3,
        "competition_degree": 99.8,
        "supply_demand_ratio": 1.4
      },
      "profit_data": {
        "margin": 42.3,
        "avg_price": 19.99,
        "conversion_rate": 11.2
      },
      "risk_data": {
        "compliance_level": "中",
        "return_rate": 8.2,
        "rating": 4.2
      },
      "tags": ["🔥高增长", "💰高利润", "⚡快速起量"],
      "opportunity_type": "蓝海机会"
    }
  ]
}
```

**行业天花板数据结构** (ceiling-analysis.json):

```json
{
  "categories": [
    {
      "category": "宠物用品",
      "subcategory": "自动喂食器",
      "market_scale": {
        "current_size": 136.8,
        "future_size": 206.7,
        "cagr_5y": 8.61,
        "monthly_sales": 1000000,
        "product_count": 400
      },
      "consumer_profile": {
        "age_distribution": {
          "under_40": 49,
          "millennials": 33,
          "gen_z": 16
        },
        "ownership_rate": 66,
        "spending_willingness": "高"
      },
      "market_structure": {
        "monopoly_index": 4,
        "cr5": 28.9,
        "cr10": 59.16,
        "new_product_ratio": 18,
        "signal": "🟡"
      },
      "operation_maturity": {
        "a_plus_coverage": 98.5,
        "video_coverage": 85.29,
        "certification_required": ["FDA", "UL/ETL"],
        "patent_risk": "中",
        "signal": "🟡"
      },
      "ai_conclusion": {
        "recommendation": "✅ 值得进入，但需注意以下要点",
        "strengths": [
          "行业大盘稳定增长(8.61% CAGR)，天花板高",
          "目标人群年轻化(49% < 40岁)，智能化需求旺盛",
          "垄断度适中(4 < 5)，新品有18%占比，非红海"
        ],
        "challenges": [
          "品牌集中度59%，需要差异化产品策略",
          "A+和视频覆盖率超85%，内容能力是必备项",
          "专利风险较高，建议申请外观专利后再上架"
        ],
        "suitable_sellers": [
          "有A+内容制作能力或预算外包",
          "有电子产品供应链资源(FDA/UL认证渠道)",
          "能投入时间做差异化功能开发(如APP智能控制)",
          "有耐心等待专利申请(3-6个月)"
        ]
      }
    }
  ]
}
```

---

## 📋 第二部分：超详细开发 TODO 列表

### 说明

- **总计**: 200+ 条任务
- **分类**: 按模块 + 功能点组织
- **优先级**: P0 (必须) / P1 (重要) / P2 (优化)
- **依赖关系**: 明确标注前置任务
- **验收标准**: 每条任务都有明确的完成定义

---

## 🎯 Phase 0: 项目初始化与基础设施 (共20条)

### 0.1 项目结构搭建

- [ ] **TODO-001** [P0] 创建项目根目录 `product-selection-demo`
- [ ] **TODO-002** [P0] 创建 `assets/css/` 目录
- [ ] **TODO-003** [P0] 创建 `assets/js/` 目录
- [ ] **TODO-004** [P0] 创建 `assets/data/` 目录
- [ ] **TODO-005** [P0] 创建 `assets/images/` 目录
- [ ] **TODO-006** [P0] 创建 `assets/libs/` 目录
- [ ] **TODO-007** [P0] 创建 `modules/` 目录
- [ ] **TODO-008** [P0] 创建 `docs/` 目录（如未创建）

### 0.2 第三方库下载与配置

- [ ] **TODO-009** [P0] 下载 Bootstrap 5.3.x 到 `assets/libs/bootstrap.min.css`
- [ ] **TODO-010** [P0] 下载 Bootstrap Bundle JS 到 `assets/libs/bootstrap.bundle.min.js`
- [ ] **TODO-011** [P0] 下载 ECharts 5.5.x 到 `assets/libs/echarts.min.js`
- [ ] **TODO-012** [P0] 下载 Axios 到 `assets/libs/axios.min.js`
- [ ] **TODO-013** [P0] 下载 Day.js 到 `assets/libs/dayjs.min.js`
- [ ] **TODO-014** [P0] 下载 html2pdf.js 到 `assets/libs/html2pdf.bundle.min.js`

### 0.3 基础样式文件

- [ ] **TODO-015** [P0] 创建 `assets/css/variables.css` 并定义 CSS 变量
  - 定义主色系 (primary, secondary, accent, danger)
  - 定义灰度 (gray-50 ~ gray-900)
  - 定义评分颜色 (excellent, good, fair, poor)
  - 定义间距变量 (xs, sm, md, lg, xl)
  - 定义字体变量 (font-family, font-size)
  - 定义圆角变量 (border-radius)
  - 定义阴影变量 (shadow)

- [ ] **TODO-016** [P0] 创建 `assets/css/reset.css` 样式重置
  - 重置 margin, padding
  - 设置 box-sizing: border-box
  - 设置全局字体

- [ ] **TODO-017** [P0] 创建 `assets/css/common.css` 公共组件样式
  - 定义按钮样式 (.btn-primary, .btn-secondary, .btn-success, .btn-danger)
  - 定义卡片样式 (.card, .card-header, .card-body)
  - 定义表格样式 (.table, .table-striped)
  - 定义模态框样式 (.modal)
  - 定义标签样式 (.badge, .tag)
  - 定义加载动画 (.spinner)

### 0.4 基础工具函数

- [ ] **TODO-018** [P0] 创建 `assets/js/utils.js` 工具函数库
  - 实现 `formatNumber(num)` - 数字格式化 (58000000 → $58M)
  - 实现 `formatPercent(num)` - 百分比格式化 (0.185 → 185%)
  - 实现 `formatDate(date)` - 日期格式化
  - 实现 `debounce(func, delay)` - 防抖函数
  - 实现 `throttle(func, delay)` - 节流函数
  - 实现 `getScoreColor(score)` - 根据评分返回颜色 (excellent/good/fair/poor)
  - 实现 `getScoreLevel(score)` - 根据评分返回等级文本
  - 实现 `sortByKey(array, key, order)` - 数组排序

- [ ] **TODO-019** [P0] 创建 `assets/js/storage.js` 候选池存储管理
  - 实现 `addToOpportunityPool(combination)` - 添加到机会候选池
  - 实现 `removeFromOpportunityPool(id)` - 从机会候选池移除
  - 实现 `getOpportunityPool()` - 获取机会候选池
  - 实现 `clearOpportunityPool()` - 清空机会候选池
  - 实现 `addToProductPool(product)` - 产品候选池操作（类似）
  - 实现 `saveToLocalStorage(key, data)` - localStorage 保存
  - 实现 `loadFromLocalStorage(key)` - localStorage 加载

- [ ] **TODO-020** [P0] 创建 `assets/js/mcda.js` MCDA 计算引擎（空文件，Step 1 时实现）

---

## 🏗️ Phase 1: 主框架开发 (共30条)

### 1.1 index.html 主框架页面

- [ ] **TODO-021** [P0] 创建 `index.html` 基础结构
  - HTML5 doctype
  - 设置 charset 和 viewport
  - 引入 Bootstrap CSS
  - 引入 variables.css, reset.css, common.css, main.css
  - 引入 Bootstrap JS, ECharts, Axios, Day.js
  - 引入 utils.js, storage.js, mcda.js, main.js

- [ ] **TODO-022** [P0] 实现顶部导航栏
  - Logo + 产品名称 "AI选品助手"
  - 右侧用户信息占位（暂不实现登录）
  - 固定在顶部 (position: sticky)

- [ ] **TODO-023** [P0] 实现 6 步骤 Tab 导航
  - 6 个 Tab 按钮：Step 1 ~ Step 6
  - Tab 名称：机会发现、品类洞察、产品选择、利润分析、竞品拆解、策略决策
  - 当前激活 Tab 高亮显示
  - 点击 Tab 切换模块

- [ ] **TODO-024** [P0] 实现候选池侧边栏
  - 固定在右侧
  - 显示"机会候选池 (X/5)"
  - 列表显示已添加的组合
  - 每个组合显示：名称、评分、标签、操作按钮（详情/移除）
  - 底部操作：对比全部、导出报告、进入下一步

- [ ] **TODO-025** [P0] 实现主内容区 `#step-container`
  - 动态加载 Step 模块的容器
  - 最小高度 800px
  - 背景色 gray-50

- [ ] **TODO-026** [P0] 实现底部信息栏
  - 版权信息
  - 产品版本号 v1.0
  - 联系方式占位

### 1.2 main.css 主框架样式

- [ ] **TODO-027** [P0] 实现整体布局样式
  - 定义 `.app-container` 容器样式
  - 定义 `.app-header` 导航栏样式
  - 定义 `.app-tabs` Tab 导航样式
  - 定义 `.app-main` 主内容区样式
  - 定义 `.app-sidebar` 候选池侧边栏样式
  - 定义 `.app-footer` 底部样式

- [ ] **TODO-028** [P0] 实现 Tab 切换动画
  - 激活 Tab 下划线动画
  - 内容切换淡入淡出效果

- [ ] **TODO-029** [P0] 实现候选池卡片样式
  - 定义 `.pool-item` 候选池项样式
  - 定义 `.pool-item__header` 头部（名称+评分）
  - 定义 `.pool-item__tags` 标签区域
  - 定义 `.pool-item__actions` 操作按钮区域
  - 定义 `.pool-empty` 空状态提示

- [ ] **TODO-030** [P0] 实现响应式布局
  - 屏幕 < 1200px 时候选池折叠为浮动按钮
  - 屏幕 < 768px 时 Tab 导航改为下拉菜单

### 1.3 main.js 主框架逻辑

- [ ] **TODO-031** [P0] 实现全局状态管理
  - 定义 `window.AppState` 对象
  - 初始化 currentStep, opportunityPool, productPool 等

- [ ] **TODO-032** [P0] 实现 `loadStepModule(stepNumber)` 函数
  - 验证 stepNumber 范围 (1-6)
  - 使用 fetch 加载 `modules/stepN.html`
  - 将内容插入 `#step-container`
  - 动态加载 `assets/css/stepN.css`（先检查是否已加载）
  - 动态加载 `assets/js/stepN.js`（移除旧脚本再加载新脚本）
  - 更新 Tab 激活状态
  - 更新 `AppState.currentStep`
  - 触发自定义事件 `step-changed`

- [ ] **TODO-033** [P0] 实现 Tab 点击事件监听
  - 为所有 `.tab-btn` 添加点击事件
  - 点击时调用 `loadStepModule(stepNumber)`

- [ ] **TODO-034** [P0] 实现候选池 UI 更新函数 `updateOpportunityPoolUI()`
  - 从 localStorage 读取候选池数据
  - 渲染候选池列表
  - 绑定"详情"按钮事件（触发 Step 内详情弹窗）
  - 绑定"移除"按钮事件（调用 storage.js 移除函数）
  - 更新候选池计数 (X/5)

- [ ] **TODO-035** [P0] 实现候选池"对比全部"功能
  - 点击"对比全部"按钮
  - 调用 Step 1 的对比功能（触发自定义事件 `compare-opportunities`）

- [ ] **TODO-036** [P0] 实现候选池"导出报告"功能
  - 点击"导出报告"按钮
  - 调用 html2pdf.js 生成 PDF
  - 包含候选池所有组合的详细信息

- [ ] **TODO-037** [P0] 实现候选池"进入下一步"功能
  - 点击"进入下一步"按钮
  - 验证候选池至少有 1 个组合
  - 调用 `loadStepModule(currentStep + 1)`

- [ ] **TODO-038** [P0] 实现错误提示函数 `showErrorMessage(message)`
  - 使用 Bootstrap Toast 显示错误信息

- [ ] **TODO-039** [P0] 实现成功提示函数 `showSuccessMessage(message)`
  - 使用 Bootstrap Toast 显示成功信息

- [ ] **TODO-040** [P0] 实现加载动画函数 `showLoading()` / `hideLoading()`
  - 显示全屏加载动画
  - 数据加载完成后隐藏

- [ ] **TODO-041** [P0] 实现页面初始化
  - DOMContentLoaded 事件监听
  - 初始化全局状态
  - 从 localStorage 恢复候选池
  - 默认加载 Step 1

### 1.4 主框架测试

- [ ] **TODO-042** [P1] 测试 Tab 切换功能
  - 点击不同 Tab 验证模块切换正常
  - 验证 CSS 和 JS 正确加载
  - 验证激活状态高亮

- [ ] **TODO-043** [P1] 测试候选池添加/移除
  - 手动触发添加操作（调用 storage API）
  - 验证候选池 UI 实时更新
  - 验证移除功能正常
  - 验证 localStorage 持久化

- [ ] **TODO-044** [P1] 测试响应式布局
  - 在不同屏幕尺寸下测试（1920px, 1200px, 768px, 375px）
  - 验证候选池折叠功能
  - 验证 Tab 导航适配

- [ ] **TODO-045** [P1] 测试错误处理
  - 模拟加载失败（错误的文件路径）
  - 验证错误提示显示
  - 验证不会导致页面崩溃

- [ ] **TODO-046** [P2] 浏览器兼容性测试
  - Chrome 最新版
  - Safari 最新版
  - Firefox 最新版
  - Edge 最新版

- [ ] **TODO-047** [P2] 性能优化检查
  - 验证模块按需加载（不切换到 Step 2 时不加载 step2.js）
  - 验证 CSS/JS 不重复加载

- [ ] **TODO-048** [P2] 无障碍测试
  - 验证键盘导航（Tab 键、Enter 键）
  - 验证 ARIA 属性正确设置

- [ ] **TODO-049** [P2] 本地服务器启动测试
  - 使用 Live Server 启动
  - 验证所有资源加载正常（无 404 错误）

- [ ] **TODO-050** [P2] Git 版本控制
  - 初始化 Git 仓库
  - 添加 .gitignore（排除 node_modules, .DS_Store 等）
  - 提交主框架代码 "feat: 完成主框架开发"

---

## 🎯 Phase 2: Step 1 机会发现阶段开发 (共150条)

> **重要说明**：Step 1 是最复杂的模块，包含行业天花板分析、热力图、组合详情、Top 20 推荐、候选池管理等多个子功能

### 2.1 样例数据准备 (20条)

#### 2.1.1 组合数据 combinations.json

- [ ] **TODO-051** [P0] 创建 `assets/data/combinations.json` 文件
- [ ] **TODO-052** [P0] 定义 JSON Schema（参考 1.8 节）
- [ ] **TODO-053** [P0] 准备 30 条组合样例数据（覆盖不同品类/国家/平台）
  - 3C配件 × US × Amazon/TikTok/Temu/AliExpress (4条)
  - 宠物用品 × US/JP × Amazon/TikTok (4条)
  - 家居装饰 × UK/US × Temu/Amazon (4条)
  - 美妆个护 × AE/US × Amazon (2条)
  - 时尚配饰 × UK/US × TikTok/Amazon (4条)
  - 户外运动 × US/JP × Amazon (2条)
  - 母婴用品 × US/UK × Amazon (2条)
  - 玩具 × VN/US × TikTok (2条)
  - 厨房用品 × US/UK × Amazon (2条)
  - 健康美容 × US × Amazon/TikTok (2条)
  - 其他品类各 1-2 条

- [ ] **TODO-054** [P0] 为每条组合生成 MCDA 综合评分（范围 65-85 分）
- [ ] **TODO-055** [P0] 为每条组合生成 5 维度评分（MA/CF/PP/RF/RC）
  - 确保评分合理分布（避免全部 80+ 或全部 60-）
  - 确保至少有 3 条"蓝海机会"（CF > 75, CR5 < 40%, 垄断度 < 3）
  - 确保至少有 3 条"高增长机会"（MA > 80, growth_rate > 150%）
  - 确保至少有 3 条"高利润机会"（PP > 80, margin > 45%）

- [ ] **TODO-056** [P0] 为每条组合生成 market_data（市场数据）
  - gmv: 范围 20M - 150M
  - growth_rate: 范围 80% - 250%
  - search_volume: 范围 50K - 300K
  - monthly_sales: 范围 500K - 2M
  - seller_count: 范围 3000 - 15000

- [ ] **TODO-057** [P0] 为每条组合生成 competition_data（竞争数据）
  - cr5: 范围 25% - 75%
  - top1_share: 范围 5% - 25%
  - monopoly_index: 范围 1.5 - 6.5（确保有 3 条 > 5 触发一票否决）
  - competition_degree: 范围 60 - 120
  - supply_demand_ratio: 范围 0.8 - 2.0

- [ ] **TODO-058** [P0] 为每条组合生成 profit_data（利润数据）
  - margin: 范围 25% - 65%
  - avg_price: 范围 9.99 - 89.99
  - conversion_rate: 范围 5% - 18%

- [ ] **TODO-059** [P0] 为每条组合生成 risk_data（风险数据）
  - compliance_level: "低" / "中" / "高"
  - return_rate: 范围 5% - 15%
  - rating: 范围 3.8 - 4.7

- [ ] **TODO-060** [P0] 为每条组合生成 tags 标签
  - 根据数据动态生成（高增长、高利润、蓝海机会、快速起量等）
  - 每条组合 1-3 个标签

- [ ] **TODO-061** [P0] 为每条组合生成 opportunity_type 分类
  - 蓝海机会 / 红海竞争 / 增长机会 / 利润机会 / 平衡机会

- [ ] **TODO-062** [P0] 验证 combinations.json 数据完整性
  - 所有必填字段存在
  - 数据类型正确（数字/字符串/对象）
  - 评分范围合理（0-100）

#### 2.1.2 行业天花板数据 ceiling-analysis.json

- [ ] **TODO-063** [P0] 创建 `assets/data/ceiling-analysis.json` 文件
- [ ] **TODO-064** [P0] 定义 JSON Schema（参考 1.8 节）
- [ ] **TODO-065** [P0] 准备 5 个品类的天花板数据
  - 宠物用品 > 自动喂食器
  - 3C配件 > 无线充电器
  - 家居装饰 > 智能香薰机
  - 美妆个护 > 洁面仪
  - 时尚配饰 > 智能手表表带

- [ ] **TODO-066** [P0] 为每个品类生成 market_scale 数据
  - current_size: 当前市场规模（单位：亿美元）
  - future_size: 5 年后市场规模
  - cagr_5y: 5 年 CAGR（范围 3% - 15%）
  - monthly_sales: 月销售额（单位：万美元）
  - product_count: 产品数量

- [ ] **TODO-067** [P0] 为每个品类生成 consumer_profile 数据
  - age_distribution: 年龄分布（under_40, millennials, gen_z）
  - ownership_rate: 养宠/使用率（%）
  - spending_willingness: 消费意愿（高/中/低）

- [ ] **TODO-068** [P0] 为每个品类生成 market_structure 数据
  - monopoly_index: 垄断度（1-7）
  - cr5: 前 5 名集中度（%）
  - cr10: 前 10 名集中度（%）
  - new_product_ratio: 新品占比（%）
  - signal: 信号灯（🟢/🟡/🔴）

- [ ] **TODO-069** [P0] 为每个品类生成 operation_maturity 数据
  - a_plus_coverage: A+ 覆盖率（%）
  - video_coverage: 视频覆盖率（%）
  - certification_required: 认证要求数组
  - patent_risk: 专利风险（低/中/高）
  - signal: 信号灯（🟢/🟡/🔴）

- [ ] **TODO-070** [P0] 为每个品类生成 ai_conclusion 数据
  - recommendation: 进入建议（✅/⚠️/🚫）
  - strengths: 优势数组（3-5 条）
  - challenges: 挑战数组（2-4 条）
  - suitable_sellers: 适合卖家画像（3-5 条）

---

### 2.2 MCDA 计算引擎实现 (15条)

- [ ] **TODO-071** [P0] 在 `assets/js/mcda.js` 实现 MCDA v1.0 评分算法
  - 实现 `calculateMCDAv1(combinationData, weights)` 函数
  - 输入：组合数据 + 权重对象 `{ MA, CF, PP, RF, RC }`
  - 输出：综合评分（0-100）

- [ ] **TODO-072** [P0] 实现 MA（市场吸引力）维度评分函数
  - `calculateMA(marketData)` 函数
  - 考虑因素：GMV 规模、增长率、搜索热度、市场成熟度
  - 权重：GMV 40%, 增长率 35%, 搜索热度 15%, 成熟度 10%

- [ ] **TODO-073** [P0] 实现 CF（竞争可行性）维度评分函数
  - `calculateCF(competitionData)` 函数
  - 考虑因素：CR5 集中度、垄断度、新卖家存活率、竞争程度
  - 权重：垄断度 35%, CR5 30%, 存活率 20%, 竞争程度 15%
  - 一票否决：垄断度 > 5 时，CF 评分强制 < 30

- [ ] **TODO-074** [P0] 实现 PP（利润潜力）维度评分函数
  - `calculatePP(profitData)` 函数
  - 考虑因素：毛利率、客单价、转化率
  - 权重：毛利率 50%, 客单价 30%, 转化率 20%

- [ ] **TODO-075** [P0] 实现 RF（资源匹配度）维度评分函数
  - `calculateRF(combinationData)` 函数
  - 考虑因素：供应链要求、内容能力要求、资金要求、认证门槛
  - 权重：供应链 40%, 内容能力 30%, 资金 20%, 认证 10%
  - 备注：POC 阶段可简化为基于平台/品类的固定评分

- [ ] **TODO-076** [P0] 实现 RC（风险控制）维度评分函数
  - `calculateRC(riskData)` 函数
  - 考虑因素：合规风险、退货率、评分、平台政策稳定性
  - 权重：合规 35%, 退货率 30%, 评分 20%, 平台稳定性 15%

- [ ] **TODO-077** [P0] 实现一票否决机制
  - `applyVetoMechanism(scores)` 函数
  - 检查是否有维度评分 < 25
  - 如果有，总评分 × 0.7（惩罚系数）

- [ ] **TODO-078** [P0] 实现协同加成机制
  - `applySynergyBonus(scores)` 函数
  - 检查是否有多个维度 > 85
  - 如果有 3 个及以上，总评分 × 1.05（奖励系数）

- [ ] **TODO-079** [P0] 实现综合评分计算
  - `calculateOverallScore(scores, weights, vetoPenalty, synergyBonus)` 函数
  - 公式：Σ(维度分 × 权重) × 惩罚系数 × 奖励系数
  - 确保最终评分范围 0-100

- [ ] **TODO-080** [P0] 实现权重归一化函数
  - `normalizeWeights(weights)` 函数
  - 确保所有权重之和 = 100%
  - 如果不等于 100%，按比例调整

- [ ] **TODO-081** [P0] 实现默认权重配置
  - 定义 5 种战略模式的权重配置（参考 02 文档 2.5.3 节）
  - 综合最优、高增长优先、高利润优先、蓝海机会、快速起量

- [ ] **TODO-082** [P0] 实现评分缓存机制
  - 使用 Map 缓存已计算过的评分
  - Key: `${combinationId}_${weightsHash}`
  - 避免重复计算

- [ ] **TODO-083** [P1] 编写 MCDA 计算单元测试
  - 测试每个维度评分函数
  - 测试一票否决机制
  - 测试协同加成机制
  - 测试综合评分计算

- [ ] **TODO-084** [P1] 验证 MCDA 评分结果合理性
  - 用 combinations.json 中的 30 条数据测试
  - 对比预期评分和计算评分（误差 < 5 分）

- [ ] **TODO-085** [P2] 性能优化
  - 批量计算 30 条组合评分耗时 < 100ms

---

### 2.3 Step 1 HTML 结构 (25条)

- [ ] **TODO-086** [P0] 创建 `modules/step1.html` 文件
- [ ] **TODO-087** [P0] 实现顶部标题区
  - 标题："Step 1: 机会发现"
  - 副标题："从 480 个组合中发现高潜力市场机会"

#### 2.3.1 行业天花板分析模块

- [ ] **TODO-088** [P0] 实现品类选择器
  - 下拉菜单，显示 5 个已准备数据的品类
  - 选择后触发天花板数据加载

- [ ] **TODO-089** [P0] 实现四维度判断卡片容器
  - 2x2 网格布局
  - 每个卡片包含：维度标题、信号灯、关键指标、结论

- [ ] **TODO-090** [P0] 实现"行业规模与增长空间"卡片
  - 显示：市场规模、5 年 CAGR、月销售额、产品数量
  - 信号灯逻辑：CAGR > 8% 🟢, 3-8% 🟡, < 3% 🔴
  - 结论文本：增长潜力大/稳定增长/成熟衰退

- [ ] **TODO-091** [P0] 实现"目标消费人群特征"卡片
  - 显示：年龄分布、养宠/使用率、消费特征
  - 信号灯逻辑：40 岁以下占比 > 40% 🟢, 20-40% 🟡, < 20% 🔴
  - 结论文本：人群画像清晰/需进一步细分/市场不明确

- [ ] **TODO-092** [P0] 实现"市场结构与新品机会"卡片
  - 显示：品牌集中度、垄断度、新品占比
  - 信号灯逻辑：垄断度 < 3 🟢, 3-5 🟡, > 5 🔴（一票否决）
  - 结论文本：蓝海机会/竞争适中/红海垄断

- [ ] **TODO-093** [P0] 实现"运营成熟度与门槛"卡片
  - 显示：A+ 覆盖率、视频覆盖率、认证要求、专利风险
  - 信号灯逻辑：A+ 覆盖率 < 70% 🟢, 70-90% 🟡, > 90% 🔴
  - 结论文本：门槛低/需一定能力/高门槛

- [ ] **TODO-094** [P0] 实现 AI 综合判断卡片
  - 显示：进入建议（✅/⚠️/🚫）
  - 显示：优势列表（3-5 条）
  - 显示：挑战列表（2-4 条）
  - 显示：适合卖家画像（3-5 条）
  - 显示：不适合类型（2-3 条）

- [ ] **TODO-095** [P0] 实现详细数据展开区域（可折叠）
  - Google Trends 趋势详情（ECharts 折线图占位）
  - BSR 历史趋势（ECharts 折线图占位）
  - 产品分类与价格带（ECharts 柱状图占位）

- [ ] **TODO-096** [P0] 实现底部操作栏
  - "✅ 通过预判，进入热力图选组合"按钮
  - "❌ 换个品类"按钮

#### 2.3.2 市场机会热力图模块

- [ ] **TODO-097** [P0] 实现左侧工具栏
  - 视图模式单选按钮（按国家/平台/品类展开）
  - 评分维度单选按钮（综合评分/MA/CF/PP/RF/RC）
  - 快速筛选复选框（仅显示蓝海/高增长/高利润/排除高风险）

- [ ] **TODO-098** [P0] 实现热力图主容器
  - `<div id="heatmap-chart" style="width: 100%; height: 800px;"></div>`
  - 用于渲染 ECharts 热力图

- [ ] **TODO-099** [P0] 实现热力图颜色说明
  - 显示评分区间和对应颜色
  - 90-100 红色（顶级机会）
  - 80-90 橙色（优质机会）
  - 70-80 黄色（中等机会）
  - 60-70 蓝色（一般机会）
  - < 60 灰色（谨慎进入）

- [ ] **TODO-100** [P0] 实现热力图交互说明
  - 点击格子展开详情面板
  - 鼠标悬停显示快速预览卡片
  - 右键菜单（加入候选池/对比/查看趋势）

#### 2.3.3 组合详情面板模块

- [ ] **TODO-101** [P0] 实现详情面板容器（Bootstrap Modal）
  - Modal 标题：组合名称（品类 × 国家 × 平台）
  - Modal 尺寸：特大（xl）

- [ ] **TODO-102** [P0] 实现顶部概览卡片
  - 综合评分 + 颜色块
  - 机会类型标签（🔥高增长 💰高利润等）
  - 风险等级
  - AI 洞察文本

- [ ] **TODO-103** [P0] 实现五维雷达图容器
  - `<div id="radar-chart" style="width: 100%; height: 300px;"></div>`

- [ ] **TODO-104** [P0] 实现市场规模卡片
  - 显示：GMV、增长率、搜索量、月销量、供需比
  - 与行业基准对比（⬆️优于基准/⬇️低于基准）

- [ ] **TODO-105** [P0] 实现竞争格局卡片
  - 显示：CR5、卖家数、存活率、垄断度、竞争度
  - 与行业基准对比

- [ ] **TODO-106** [P0] 实现利润指标卡片
  - 显示：毛利率、客单价、转化率

- [ ] **TODO-107** [P0] 实现风险指标卡片
  - 显示：合规风险、退货率、评分

- [ ] **TODO-108** [P0] 实现详细数据表格（可折叠）
  - 9 张表：market_overview, competition, sellers, profit, pricing, traffic, product_performance, risk, mcda_scores
  - 每张表默认折叠，点击展开

- [ ] **TODO-109** [P0] 实现底部操作栏
  - "✅ 加入候选池"按钮
  - "📊 对比其他组合"按钮
  - "📈 查看趋势"按钮（占位）
  - "❌ 关闭"按钮

#### 2.3.4 Top 20 推荐清单模块

- [ ] **TODO-110** [P0] 实现策略模式选择器
  - 单选按钮组：综合最优/高增长优先/高利润优先/蓝海机会/快速起量
  - 选择后触发重新排序

- [ ] **TODO-111** [P0] 实现 Top 20 清单表格
  - 表头：排名、组合、综合分、核心标签、一句话推荐、操作
  - 表格可排序（点击表头）
  - 表格可分页（每页 10 条）

- [ ] **TODO-112** [P0] 实现批量操作区
  - "✅ 全部加入候选池"按钮
  - "📊 对比前 5 名"按钮
  - "📥 导出 Excel"按钮（占位）

#### 2.3.5 组合对比模块

- [ ] **TODO-113** [P0] 实现对比面板容器（Bootstrap Modal）
  - Modal 标题："组合对比分析"
  - Modal 尺寸：特大（xl）

- [ ] **TODO-114** [P0] 实现对比表格
  - 表头：对比维度、组合 1、组合 2、组合 3（最多 5 个）
  - 表体：综合评分、五维评分、核心指标
  - 最优值高亮显示（✅ 标记）

- [ ] **TODO-115** [P0] 实现 AI 对比总结
  - 为每个组合生成一句话总结
  - 基于数据动态生成

---

### 2.4 Step 1 CSS 样式 (15条)

- [ ] **TODO-116** [P0] 创建 `assets/css/step1.css` 文件

- [ ] **TODO-117** [P0] 实现行业天花板分析样式
  - 定义 `.ceiling-analysis` 容器样式
  - 定义 `.ceiling-card` 卡片样式
  - 定义 `.signal-light` 信号灯样式（🟢/🟡/🔴）
  - 定义 `.ceiling-conclusion` 结论区域样式

- [ ] **TODO-118** [P0] 实现热力图工具栏样式
  - 定义 `.heatmap-toolbar` 样式
  - 定义 `.view-mode-selector` 样式
  - 定义 `.dimension-selector` 样式
  - 定义 `.filter-checkbox` 样式

- [ ] **TODO-119** [P0] 实现热力图容器样式
  - 定义 `.heatmap-container` 样式
  - 定义 `.heatmap-legend` 颜色说明样式
  - 定义 `.heatmap-interaction-hint` 交互提示样式

- [ ] **TODO-120** [P0] 实现组合详情面板样式
  - 定义 `.combination-detail-modal` 样式
  - 定义 `.overview-card` 顶部概览卡片样式
  - 定义 `.metric-card` 指标卡片样式（市场规模、竞争格局等）
  - 定义 `.data-table-collapsible` 可折叠表格样式

- [ ] **TODO-121** [P0] 实现 Top 20 清单样式
  - 定义 `.top20-container` 样式
  - 定义 `.strategy-selector` 策略选择器样式
  - 定义 `.top20-table` 表格样式
  - 定义 `.rank-badge` 排名徽章样式（金/银/铜）

- [ ] **TODO-122** [P0] 实现对比面板样式
  - 定义 `.comparison-modal` 样式
  - 定义 `.comparison-table` 对比表格样式
  - 定义 `.best-value-highlight` 最优值高亮样式

- [ ] **TODO-123** [P0] 实现标签样式
  - 定义 `.tag-high-growth` 高增长标签（🔥）
  - 定义 `.tag-high-profit` 高利润标签（💰）
  - 定义 `.tag-blue-ocean` 蓝海机会标签（🌊）
  - 定义 `.tag-fast-launch` 快速起量标签（⚡）

- [ ] **TODO-124** [P0] 实现评分颜色样式
  - 定义 `.score-excellent` (90-100 分)
  - 定义 `.score-good` (80-90 分)
  - 定义 `.score-fair` (70-80 分)
  - 定义 `.score-poor` (< 70 分)

- [ ] **TODO-125** [P0] 实现响应式样式
  - 屏幕 < 1200px 时热力图工具栏折叠
  - 屏幕 < 768px 时卡片堆叠布局

- [ ] **TODO-126** [P1] 实现动画效果
  - 卡片悬停放大效果 (transform: scale(1.02))
  - 按钮点击波纹效果
  - 面板展开/折叠过渡动画

- [ ] **TODO-127** [P1] 实现深色模式适配（可选）
  - 定义深色模式 CSS 变量
  - 使用 `@media (prefers-color-scheme: dark)` 检测

- [ ] **TODO-128** [P2] 打印样式优化
  - 定义 `@media print` 样式
  - 隐藏工具栏和操作按钮
  - 优化表格打印布局

- [ ] **TODO-129** [P2] 无障碍优化
  - 确保对比度符合 WCAG AA 标准
  - 为图标添加文字说明（screen reader）

- [ ] **TODO-130** [P2] 性能优化
  - 使用 CSS `will-change` 优化动画性能
  - 避免大面积重绘（使用 transform 代替 left/top）

---

### 2.5 Step 1 JavaScript 业务逻辑 (70条)

- [ ] **TODO-131** [P0] 创建 `assets/js/step1.js` 文件

#### 2.5.1 数据加载模块

- [ ] **TODO-132** [P0] 实现数据加载函数 `loadCombinationsData()`
  - 使用 Axios 加载 `assets/data/combinations.json`
  - 错误处理：显示错误提示
  - 返回 Promise<Array>

- [ ] **TODO-133** [P0] 实现数据加载函数 `loadCeilingAnalysisData()`
  - 使用 Axios 加载 `assets/data/ceiling-analysis.json`
  - 错误处理：显示错误提示
  - 返回 Promise<Array>

- [ ] **TODO-134** [P0] 实现数据缓存机制
  - 定义 `window.Step1Data = { combinations: [], ceilingAnalysis: [] }`
  - 首次加载后缓存到全局变量
  - 后续直接从缓存读取

#### 2.5.2 行业天花板分析模块

- [ ] **TODO-135** [P0] 实现品类选择器事件监听
  - 监听 `#category-selector` 的 change 事件
  - 触发 `loadCeilingAnalysis(category)` 函数

- [ ] **TODO-136** [P0] 实现 `loadCeilingAnalysis(category)` 函数
  - 从缓存中查找对应品类数据
  - 调用 `renderCeilingAnalysis(data)` 渲染

- [ ] **TODO-137** [P0] 实现 `renderCeilingAnalysis(data)` 函数
  - 渲染四维度判断卡片
  - 渲染 AI 综合判断卡片
  - 更新信号灯颜色

- [ ] **TODO-138** [P0] 实现信号灯判断逻辑函数
  - `getScaleSizeSignal(cagr)` - 行业规模信号灯
  - `getConsumerSignal(ageUnder40)` - 消费人群信号灯
  - `getMarketStructureSignal(monopolyIndex)` - 市场结构信号灯
  - `getOperationMaturitySignal(aPlusCoverage)` - 运营成熟度信号灯

- [ ] **TODO-139** [P0] 实现详细数据展开功能
  - 监听"展开详情"按钮点击
  - 渲染 Google Trends 趋势图（ECharts 折线图）
  - 渲染 BSR 历史趋势图（ECharts 折线图）
  - 渲染产品分类与价格带图（ECharts 柱状图）

- [ ] **TODO-140** [P0] 实现"通过预判，进入热力图选组合"按钮功能
  - 隐藏天花板分析模块
  - 显示热力图模块
  - 自动筛选该品类的组合并高亮

- [ ] **TODO-141** [P0] 实现"换个品类"按钮功能
  - 重置品类选择器
  - 清空天花板分析数据

#### 2.5.3 热力图渲染模块

- [ ] **TODO-142** [P0] 实现 `initHeatmap()` 函数
  - 初始化 ECharts 实例
  - 绑定到 `#heatmap-chart` 容器
  - 调用 `renderHeatmap(data, viewMode, dimension)` 渲染

- [ ] **TODO-143** [P0] 实现 `renderHeatmap(data, viewMode, dimension)` 函数
  - 根据 viewMode 转换数据结构（按平台/国家/品类展开）
  - 根据 dimension 选择评分维度（综合/MA/CF/PP/RF/RC）
  - 配置 ECharts option
  - 调用 `chart.setOption(option)`

- [ ] **TODO-144** [P0] 实现热力图数据转换函数
  - `transformDataByPlatform(data)` - 按平台展开
    - Y 轴：品类 × 国家组合（120 行）
    - X 轴：平台（4 列）
  - `transformDataByCountry(data)` - 按国家展开
    - Y 轴：品类 × 平台组合（80 行）
    - X 轴：国家（6 列）
  - `transformDataByCategory(data)` - 按品类展开
    - Y 轴：国家 × 平台组合（24 行）
    - X 轴：品类（20 列）

- [ ] **TODO-145** [P0] 实现热力图 ECharts 配置
  - 配置 tooltip（鼠标悬停提示）
    - 显示：组合名称、评分、GMV、增长率、CR5
  - 配置 visualMap（颜色映射）
    - 90-100: #d94e5d (红色)
    - 80-90: #eac736 (橙色)
    - 70-80: #50a3ba (黄色)
    - 60-70: #adb5bd (蓝色)
    - < 60: #e9ecef (灰色)
  - 配置 xAxis 和 yAxis
  - 配置 series.data（热力图数据点）

- [ ] **TODO-146** [P0] 实现热力图单元格点击事件
  - 监听 chart 的 'click' 事件
  - 获取点击的组合数据
  - 调用 `showCombinationDetail(combination)` 显示详情

- [ ] **TODO-147** [P0] 实现热力图右键菜单
  - 监听 chart 的 'contextmenu' 事件
  - 显示自定义菜单：加入候选池 / 对比 / 查看趋势
  - 防止浏览器默认右键菜单

- [ ] **TODO-148** [P0] 实现视图模式切换
  - 监听视图模式单选按钮 change 事件
  - 调用 `renderHeatmap(data, newViewMode, currentDimension)`

- [ ] **TODO-149** [P0] 实现评分维度切换
  - 监听评分维度单选按钮 change 事件
  - 调用 `renderHeatmap(data, currentViewMode, newDimension)`

- [ ] **TODO-150** [P0] 实现快速筛选功能
  - 监听快速筛选复选框 change 事件
  - 调用 `filterCombinations(data, filters)` 过滤数据
  - 调用 `renderHeatmap(filteredData, viewMode, dimension)` 重新渲染

- [ ] **TODO-151** [P0] 实现筛选逻辑函数
  - `filterBlueOcean(combinations)` - 蓝海机会
    - CF > 75 且 CR5 < 40% 且 垄断度 < 3
  - `filterHighGrowth(combinations)` - 高增长
    - MA > 80 且 growth_rate > 150% 且 supply_demand_ratio > 1.2
  - `filterHighProfit(combinations)` - 高利润
    - PP > 80 且 margin > 45%
  - `filterLowRisk(combinations)` - 排除高风险
    - RC > 50 且 垄断度 < 5

#### 2.5.4 组合详情面板模块

- [ ] **TODO-152** [P0] 实现 `showCombinationDetail(combination)` 函数
  - 获取组合数据
  - 调用各子渲染函数
  - 显示 Modal

- [ ] **TODO-153** [P0] 实现顶部概览卡片渲染
  - `renderOverviewCard(combination)` 函数
  - 显示综合评分 + 颜色块
  - 显示标签（高增长/高利润等）
  - 显示风险等级
  - 显示 AI 洞察（调用 `generateAIInsight(combination)`）

- [ ] **TODO-154** [P0] 实现 AI 洞察生成函数
  - `generateAIInsight(combination)` 函数
  - 基于模板动态生成（参考 02 文档 2.4.4 节）
  - 示例："该组合在 {platform} 平台表现 {评价}, 市场增长率达 {growth_rate}%, 竞争 {竞争程度}..."

- [ ] **TODO-155** [P0] 实现五维雷达图渲染
  - `renderRadarChart(combination)` 函数
  - 初始化 ECharts 实例（绑定到 `#radar-chart`）
  - 配置雷达图 option
    - indicator: MA, CF, PP, RF, RC（每个最大值 100）
    - series.data: 五维评分数组

- [ ] **TODO-156** [P0] 实现雷达图交互
  - 鼠标悬停到某个维度
  - 显示该维度的详细构成（tooltip）
  - 示例：MA = GMV 评分 85 + 增长率评分 92 + 搜索热度评分 78

- [ ] **TODO-157** [P0] 实现市场规模卡片渲染
  - `renderMarketCard(combination)` 函数
  - 显示：GMV、增长率、搜索量、月销量、供需比
  - 与行业基准对比（⬆️/⬇️）

- [ ] **TODO-158** [P0] 实现竞争格局卡片渲染
  - `renderCompetitionCard(combination)` 函数
  - 显示：CR5、卖家数、存活率、垄断度、竞争度
  - 与行业基准对比

- [ ] **TODO-159** [P0] 实现利润指标卡片渲染
  - `renderProfitCard(combination)` 函数
  - 显示：毛利率、客单价、转化率

- [ ] **TODO-160** [P0] 实现风险指标卡片渲染
  - `renderRiskCard(combination)` 函数
  - 显示：合规风险、退货率、评分

- [ ] **TODO-161** [P0] 实现详细数据表格渲染（可折叠）
  - `renderDataTables(combination)` 函数
  - 渲染 9 张表：market_overview, competition, sellers, profit, pricing, traffic, product_performance, risk, mcda_scores
  - 默认折叠，点击表头展开

- [ ] **TODO-162** [P0] 实现表格折叠/展开逻辑
  - 监听表头点击事件
  - 切换表体显示/隐藏
  - 切换图标（▼/▶）

- [ ] **TODO-163** [P0] 实现"加入候选池"按钮功能
  - 监听按钮点击
  - 验证候选池未满（< 5 个）
  - 调用 `AppStorage.addToOpportunityPool(combination)`
  - 显示成功提示
  - 触发全局事件 `opportunity-added`
  - 关闭 Modal

- [ ] **TODO-164** [P0] 实现"对比其他组合"按钮功能
  - 监听按钮点击
  - 将当前组合加入对比列表（临时数组）
  - 关闭当前 Modal
  - 提示用户"请继续选择要对比的组合（最多 5 个）"

- [ ] **TODO-165** [P0] 实现"查看趋势"按钮功能（占位）
  - 监听按钮点击
  - 显示提示"趋势功能开发中"

- [ ] **TODO-166** [P0] 实现"关闭"按钮功能
  - 监听按钮点击
  - 关闭 Modal

#### 2.5.5 Top 20 推荐清单模块

- [ ] **TODO-167** [P0] 实现 `initTop20List()` 函数
  - 加载组合数据
  - 调用 `renderTop20List(data, strategy)` 渲染

- [ ] **TODO-168** [P0] 实现 `renderTop20List(data, strategy)` 函数
  - 根据 strategy 调整 MCDA 权重
  - 重新计算所有组合评分
  - 排序（评分降序）
  - 取前 20 条
  - 渲染表格

- [ ] **TODO-169** [P0] 实现策略模式权重配置
  - 定义 `STRATEGY_WEIGHTS` 常量（参考 02 文档 2.5.3 节）
  - 综合最优：{ MA: 25, CF: 20, PP: 30, RF: 15, RC: 10 }
  - 高增长优先：{ MA: 40, CF: 15, PP: 20, RF: 15, RC: 10 }
  - 高利润优先：{ MA: 15, CF: 15, PP: 45, RF: 15, RC: 10 }
  - 蓝海机会：{ MA: 20, CF: 40, PP: 20, RF: 10, RC: 10 }
  - 快速起量：{ MA: 20, CF: 20, PP: 20, RF: 30, RC: 10 }

- [ ] **TODO-170** [P0] 实现策略选择器事件监听
  - 监听策略单选按钮 change 事件
  - 调用 `renderTop20List(data, newStrategy)`

- [ ] **TODO-171** [P0] 实现一句话推荐生成函数
  - `generateOneSentenceRecommendation(combination)` 函数
  - 基于模板动态生成（参考 02 文档 2.5.4 节）
  - 示例："高增长 + 高利润, 适合有供应链优势的卖家"

- [ ] **TODO-172** [P0] 实现推荐生成规则
  - MA > 80 且 PP > 80 → "高增长 + 高利润"
  - CF > 80 且 RC > 75 → "蓝海市场, 风险可控"
  - MA > 80 且 RF > 75 → "市场红利明显, 资源匹配度高"
  - PP > 80 且 platform = TikTok → "高利润, 适合内容型卖家"

- [ ] **TODO-173** [P0] 实现表格排名徽章渲染
  - 第 1 名：金牌 🥇
  - 第 2 名：银牌 🥈
  - 第 3 名：铜牌 🥉
  - 其他：数字排名

- [ ] **TODO-174** [P0] 实现表格行点击"详情"功能
  - 监听"详情"按钮点击
  - 调用 `showCombinationDetail(combination)`

- [ ] **TODO-175** [P0] 实现表格行点击"候选池"功能
  - 监听"候选池"按钮点击
  - 调用 `AppStorage.addToOpportunityPool(combination)`
  - 显示成功提示

- [ ] **TODO-176** [P0] 实现"全部加入候选池"功能
  - 监听按钮点击
  - 遍历 Top 20 列表
  - 批量添加到候选池（验证不超过 5 个）
  - 显示成功提示

- [ ] **TODO-177** [P0] 实现"对比前 5 名"功能
  - 监听按钮点击
  - 取前 5 名组合
  - 调用 `showComparisonModal(combinations)`

- [ ] **TODO-178** [P0] 实现"导出 Excel"功能（占位）
  - 监听按钮点击
  - 显示提示"导出功能开发中"

#### 2.5.6 组合对比模块

- [ ] **TODO-179** [P0] 实现 `showComparisonModal(combinations)` 函数
  - 验证 combinations 数量（2-5 个）
  - 调用 `renderComparisonTable(combinations)` 渲染
  - 调用 `generateComparisonSummary(combinations)` 生成 AI 总结
  - 显示 Modal

- [ ] **TODO-180** [P0] 实现 `renderComparisonTable(combinations)` 函数
  - 动态生成表格列（每个组合一列）
  - 渲染行：综合评分、五维评分、核心指标
  - 每行标记最优值（✅）

- [ ] **TODO-181** [P0] 实现最优值标记逻辑
  - `markBestValue(values, higherIsBetter)` 函数
  - higherIsBetter = true：最大值标记 ✅
  - higherIsBetter = false：最小值标记 ✅（如 CR5、退货率）

- [ ] **TODO-182** [P0] 实现 `generateComparisonSummary(combinations)` 函数
  - 为每个组合生成一句话总结
  - 基于对比结果动态生成
  - 示例："组合 1: 市场规模最大, 增长最快, 但竞争相对激烈, 适合快速抢占市场份额"

- [ ] **TODO-183** [P0] 实现"返回候选池"按钮功能
  - 监听按钮点击
  - 关闭 Modal

- [ ] **TODO-184** [P0] 实现"导出对比报告"功能
  - 监听按钮点击
  - 调用 html2pdf.js 生成 PDF
  - 包含对比表格和 AI 总结

#### 2.5.7 PDF 导出模块

- [ ] **TODO-185** [P0] 实现 `exportCombinationDetailPDF(combination)` 函数
  - 使用 html2pdf.js
  - 渲染详情面板内容为 PDF
  - 文件名：`组合详情_${组合名称}_${日期}.pdf`

- [ ] **TODO-186** [P0] 实现 `exportOpportunityPoolPDF()` 函数
  - 渲染候选池所有组合的详细信息
  - 包含：概览、五维雷达图、核心指标、AI 洞察
  - 文件名：`机会候选池报告_${日期}.pdf`

- [ ] **TODO-187** [P0] 实现 `exportComparisonPDF(combinations)` 函数
  - 渲染对比表格和 AI 总结
  - 文件名：`组合对比报告_${日期}.pdf`

- [ ] **TODO-188** [P1] 优化 PDF 样式
  - 定义专门的打印样式（去除工具栏、优化间距）
  - 确保 ECharts 图表正确渲染（转为图片）

#### 2.5.8 初始化与事件绑定

- [ ] **TODO-189** [P0] 实现 `initStep1()` 函数
  - 加载组合数据和天花板数据
  - 初始化热力图
  - 初始化 Top 20 清单
  - 绑定所有事件监听器

- [ ] **TODO-190** [P0] 实现全局事件监听
  - 监听 `window.addEventListener('step-changed')` 事件
  - 如果切换到 Step 1，调用 `initStep1()`

- [ ] **TODO-191** [P0] 实现候选池更新事件监听
  - 监听 `window.addEventListener('opportunity-added')` 事件
  - 更新热力图中已添加组合的标记（如高亮边框）

- [ ] **TODO-192** [P0] 实现页面卸载清理
  - 监听 `window.addEventListener('beforeunload')` 事件
  - 销毁 ECharts 实例
  - 清除事件监听器

#### 2.5.9 性能优化

- [ ] **TODO-193** [P1] 实现热力图数据虚拟化
  - 如果组合数据 > 100 条，使用虚拟滚动
  - 只渲染可见区域的数据点

- [ ] **TODO-194** [P1] 实现防抖/节流
  - 热力图 resize 事件使用防抖（300ms）
  - 筛选输入使用防抖（500ms）

- [ ] **TODO-195** [P1] 实现 ECharts 按需加载
  - 只加载需要的组件（heatmap, radar, tooltip, legend）
  - 减少 bundle 体积

- [ ] **TODO-196** [P1] 实现图片懒加载
  - 组合详情中的图片使用 Intersection Observer
  - 延迟加载非可见区域图片

- [ ] **TODO-197** [P2] 实现 Web Worker
  - 将 MCDA 评分计算移到 Web Worker
  - 避免阻塞主线程

#### 2.5.10 测试与验证

- [ ] **TODO-198** [P1] 单元测试：数据加载
  - 测试 `loadCombinationsData()` 函数
  - 模拟网络请求成功/失败

- [ ] **TODO-199** [P1] 单元测试：筛选逻辑
  - 测试 `filterBlueOcean()` 函数
  - 验证筛选结果正确

- [ ] **TODO-200** [P1] 单元测试：AI 洞察生成
  - 测试 `generateAIInsight()` 函数
  - 验证不同条件下生成的文本

- [ ] **TODO-201** [P1] 集成测试：热力图渲染
  - 测试热力图正常显示
  - 测试视图模式切换
  - 测试评分维度切换

- [ ] **TODO-202** [P1] 集成测试：详情面板
  - 测试点击热力图格子显示详情
  - 测试五维雷达图渲染
  - 测试"加入候选池"功能

- [ ] **TODO-203** [P1] 集成测试：Top 20 清单
  - 测试策略切换
  - 测试排序逻辑
  - 测试批量操作

- [ ] **TODO-204** [P1] 集成测试：对比功能
  - 测试多组合对比
  - 测试最优值标记
  - 测试 AI 总结生成

- [ ] **TODO-205** [P1] 用户测试：完整流程
  - 天花板分析 → 热力图筛选 → 查看详情 → 加入候选池 → 对比 → 导出报告
  - 验证流程顺畅，无阻塞

- [ ] **TODO-206** [P1] 性能测试
  - 30 条组合数据加载耗时 < 500ms
  - 热力图渲染耗时 < 1s
  - MCDA 评分计算耗时 < 100ms

- [ ] **TODO-207** [P1] 兼容性测试
  - Chrome/Safari/Firefox/Edge 最新版
  - iOS Safari / Android Chrome

- [ ] **TODO-208** [P2] 无障碍测试
  - 键盘导航（Tab 键、Enter 键、方向键）
  - 屏幕阅读器（VoiceOver/NVDA）

- [ ] **TODO-209** [P2] 错误处理测试
  - 模拟网络错误
  - 模拟数据格式错误
  - 验证错误提示友好

- [ ] **TODO-210** [P2] 边界情况测试
  - 候选池满（5 个）时添加新组合
  - 候选池空时导出报告
  - 对比列表只有 1 个组合

---

## 🎯 Phase 3: Step 2-6 开发规划 (待补充)

> **说明**：Step 2-6 的超详细 TODO 将在后续对话中补充，每个 Step 预计 50-80 条任务

### 预估任务量

- **Step 2 品类洞察**：约 60 条任务
- **Step 3 产品选择**：约 70 条任务
- **Step 4 利润分析**：约 50 条任务
- **Step 5 竞品拆解**：约 60 条任务
- **Step 6 策略决策**：约 80 条任务

**总计**：Phase 0-2 已规划 210 条，预计全部 6 个 Step 完成后超过 500 条任务。

---

## 📊 第三部分：开发进度跟踪

### 跟踪方式

- 每条 TODO 完成后，在前面勾选 `[x]`
- 在 Git commit message 中引用 TODO 编号
  - 示例：`git commit -m "feat: 实现热力图渲染功能 (TODO-142, TODO-143)"`

### 里程碑定义

| 里程碑 | 完成标志 | 预计工期 |
|-------|---------|---------|
| **M0: 项目初始化** | TODO-001 ~ TODO-050 全部完成 | 2 天 |
| **M1: Step 1 完成** | TODO-051 ~ TODO-210 全部完成 | 5-7 天 |
| **M2: Step 2-3 完成** | Step 2-3 所有 TODO 完成 | 7-10 天 |
| **M3: Step 4-6 完成** | Step 4-6 所有 TODO 完成 | 10-12 天 |
| **M4: 整体测试与优化** | 所有集成测试通过 | 3-5 天 |
| **M5: POC 演示就绪** | 演示脚本准备完成 | 1-2 天 |

**总预计工期**：28-36 天（约 4-5 周）

---

## 🔧 第四部分：开发规范与最佳实践

### 4.1 代码提交规范

**Commit Message 格式**：

```
<type>(<scope>): <subject>

<body>

<footer>
```

**Type 类型**：
- `feat`: 新功能
- `fix`: Bug 修复
- `style`: 样式修改（不影响功能）
- `refactor`: 重构（不新增功能，不修复 Bug）
- `test`: 测试相关
- `docs`: 文档更新
- `chore`: 构建/工具配置

**示例**：
```
feat(step1): 实现热力图渲染功能

- 实现数据转换函数 transformDataByPlatform
- 实现 ECharts 配置
- 实现视图模式切换

Refs: TODO-142, TODO-143, TODO-148
```

### 4.2 文件命名规范

- HTML: 小写 + 连字符（`step1.html`）
- CSS: 小写 + 连字符（`step1.css`）
- JavaScript: 小写 + 连字符（`mcda.js`）
- JSON: 小写 + 连字符（`combinations.json`）
- 图片: 小写 + 连字符 + 描述性名称（`logo-primary.png`）

### 4.3 代码注释规范

**函数注释**：
```javascript
/**
 * 计算 MCDA v1.0 综合评分
 * @param {Object} combinationData - 组合数据
 * @param {Object} weights - 权重配置 { MA, CF, PP, RF, RC }
 * @returns {number} 综合评分 (0-100)
 * @example
 * const score = calculateMCDAv1(data, { MA: 25, CF: 20, PP: 30, RF: 15, RC: 10 });
 */
function calculateMCDAv1(combinationData, weights) {
  // 实现...
}
```

**复杂逻辑注释**：
```javascript
// 一票否决机制：任一维度 < 25 分，总分 × 0.7
if (scores.MA < 25 || scores.CF < 25 || scores.PP < 25 ||
    scores.RF < 25 || scores.RC < 25) {
  penalty = 0.7;
}
```

### 4.4 CSS 注释规范

```css
/* ==========================================================================
   Step 1: 机会发现 - 热力图样式
   ========================================================================== */

/* 热力图容器
   ========================================================================== */
.heatmap-container {
  width: 100%;
  min-height: 800px;
  background-color: var(--gray-50);
}

/* 热力图工具栏 */
.heatmap-toolbar {
  padding: var(--spacing-md);
  background-color: white;
}
```

### 4.5 测试规范

**单元测试示例**（使用 Jest）：
```javascript
describe('MCDA 计算引擎', () => {
  test('calculateMA 应返回正确的 MA 评分', () => {
    const marketData = {
      gmv: 58000000,
      growth_rate: 185,
      search_volume: 120000
    };
    const score = calculateMA(marketData);
    expect(score).toBeGreaterThan(80);
    expect(score).toBeLessThan(90);
  });

  test('一票否决机制应正确触发', () => {
    const scores = { MA: 85, CF: 20, PP: 80, RF: 75, RC: 77 };
    const penalty = applyVetoMechanism(scores);
    expect(penalty).toBe(0.7);
  });
});
```

---

## 📖 第五部分：文档维护

### 5.1 版本更新规则

- **Minor 版本更新**（如 V1.0 → V1.1）：新增 TODO 或调整优先级
- **Major 版本更新**（如 V1.0 → V2.0）：架构级调整

### 5.2 变更记录

| 版本 | 日期 | 变更说明 | 作者 |
|------|------|---------|------|
| V1.0 | 2025-11-18 | 初版，完成整体架构设计 + Step 1 完整 TODO (210 条) | AI 架构师 |

---

## 🎯 第六部分：后续 AI 使用指南

### 6.1 对话模板

**开始新的开发任务时**：
```
你好，我需要继续开发 AI 选品助手项目。

请阅读以下文档：
1. /docs/ARCHITECTURE.md (本架构文档)
2. /docs/design/02-Step1-机会发现阶段-功能设计.md (产品设计文档)

我现在要开发的任务是：TODO-142 ~ TODO-145（热力图渲染模块）

请严格按照架构文档中的规范实现这些功能，包括：
- 文件路径
- 命名规范
- 代码风格
- 注释规范

开始实现吧！
```

### 6.2 验收标准模板

每条 TODO 完成后，验收清单：
- [ ] 代码符合命名规范
- [ ] 代码有完整注释
- [ ] 功能测试通过（手动或自动）
- [ ] 无 Console 错误
- [ ] 符合设计文档要求
- [ ] Git commit message 规范
- [ ] 勾选对应 TODO 编号

---

## 🔗 第七部分：相关资源

### 7.1 产品设计文档

- [01-整体流程和模块设计.md](./design/01-整体流程和模块设计.md)
- [02-Step1-机会发现阶段-功能设计.md](./design/02-Step1-机会发现阶段-功能设计.md)
- [03-Step2-市场与品类洞察阶段-功能设计.md](./design/03-Step2-市场与品类洞察阶段-功能设计.md)
- [04-Step3-产品选择阶段-功能设计.md](./design/04-Step3-产品选择阶段-功能设计.md)
- [05-Step4-利润分析与可行性验证阶段-功能设计.md](./design/05-Step4-利润分析与可行性验证阶段-功能设计.md)
- [06-Step5-竞品深度拆解阶段-功能设计.md](./design/06-Step5-竞品深度拆解阶段-功能设计.md)
- [07-Step6-策略决策与执行规划阶段-功能设计.md](./design/07-Step6-策略决策与执行规划阶段-功能设计.md)

### 7.2 技术文档

- [Bootstrap 5 官方文档](https://getbootstrap.com/docs/5.3/)
- [ECharts 官方文档](https://echarts.apache.org/zh/index.html)
- [Axios 官方文档](https://axios-http.com/)
- [html2pdf.js 官方文档](https://github.com/eKoopmans/html2pdf.js)

### 7.3 参考案例

- ECharts 热力图示例：[官方示例](https://echarts.apache.org/examples/zh/editor.html?c=heatmap-cartesian)
- ECharts 雷达图示例：[官方示例](https://echarts.apache.org/examples/zh/editor.html?c=radar)
- Bootstrap Modal 示例：[官方文档](https://getbootstrap.com/docs/5.3/components/modal/)

---

## 🏁 结语

本架构文档是整个项目的**北极星**，所有后续开发必须以此为准绳。

**核心理念**：
1. **模块化**：每个 Step 独立开发、独立测试、独立部署
2. **标准化**：统一的命名、注释、提交规范
3. **可追溯**：每个功能都有对应的 TODO 编号和产品设计依据
4. **可验收**：每条 TODO 都有明确的完成定义

**祝开发顺利！** 🚀

---

**文档结束**
